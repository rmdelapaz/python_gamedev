# Python Game Dev — Course Modernization TODO

> Forward-looking next-chat steering. **Read `support/session.md` first** for the immediate resume pointer; then this file for the next-chat task. Full history, decisions, review items, and resolved questions live in `status.md` — pull individual sections only on demand.

**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\python_gamedev`
(Use `\\wsl$\` paths, not `\\wsl.localhost\`.)

---

## Current state

- Phases 1–6: ✅ done. Phase 6 = 25 SVGs shipped, **all 25 render-checked clean as of chat 24** (#25 `sound-layers.svg` was the last open render-check; passed all 5 verification points at iframe-simulated 380×820 — 5th confirmation of the chat-15 `!important` hardening for inline-display canvases). #20 `game-loop-cycle.svg` (chat 14) eyeball-discharged out-of-band by Ray between chats 23 and 24 — **no "pending eyeball" items remain on any shipped SVG.**
- Phase 7: ✅ **done. 15/15 lessons wired AND render-checked clean as of chat 36** (chat 36 = FINAL Phase 7 chat: render-checked SVG #31 `tower-defense-lane.svg` clean at iframe-simulated 380×820 with all 5 verification points; **11th confirmation of the chat-15 `!important` hardening**; **second two-pattern hardening case** after chat-31 racing — canvas inline `display: inline-block` AND `background: #2d4a2b`). 0 wired-but-render-pending. **0/15 deferred. PHASE 7 CLOSED.**
- Phase 8: 🔄 **chat 41 closed at M2 done (cohort-2 partial — 2 of 4 sprite_* lessons; M3+M4 carry forward to chat 42 per the chat-41 brief's "drop the last lesson … chat-N+1 carries forward" safety-valve, triggered by interruption between M2 close and M3 start with 2 lessons dropped instead of 1); chat 42 = cohort-2 finish (2 sprite_* lessons: sheets + groups_layers) + cohort-3 stage.** Cohort 1 hit precisely as planned in chat 40 (4 pygame_basics lessons all flipped `quiz_drafted` → `done` with `completed_chat: 40` across M1–M4). Cohort 2 partial in chat 41: 2 of 4 sprite_* lessons landed (`sprite_loading_images.html` "Cache the Spin, Skip the Tax" + `sprite_animation.html` "Two States, One dt") atomically complete on disk; M3+M4 carry to chat 42 = cohort-2 finish. **7/64 lessons `done`** (pygame_basics 5/5 chat 38+40 + sprite_loading_images chat 41 + sprite_animation chat 41) + 57/64 `quiz_drafted` + 0/64 `not_started`. **9 Phase-8 findings** total at chat-41 close (unchanged from chat-40 close — cohort-2 partial surfaced no new findings; the 2 lessons that landed ran identically to cohort 1 rhythm). Goal: 64/64 lessons with real Practice Exercise + 3-question quiz content.
- Phase 9: ⏳ deferred to after Phase 8 wraps. Code quality / lint pass across all 64 lessons (mixed `@dataclass` usage, inconsistent type hints, drifting class naming, long monolithic class examples vs focused snippets). Both starter scaffolds remain further down in this file ("Phase 8 — Pedagogy upgrade" and "Phase 9 — Code quality pass").

**Two new working disciplines added chat 23 (Ray-driven, in response to 4th interrupted-artifact case):**

1. **save-first-not-last** — doc updates happen at milestone boundaries, not chat end. Stub `todo.md` + `status.md` writes at chat start; incremental updates after each milestone. Reverses the chat-19/21/22 failure pattern (saves were ordered *near* the interruption window; this moves them *away* from it).
2. **`support/session.md`** — single-paragraph resume pointer, **first file to read on chat start** before this `todo.md`. Overwritten at every milestone; does not accumulate history (`status.md`'s job). Schema in the file itself.

**Four new conventions added chat 27 (Ray-driven, in response to interruption-rate analysis), effective chat 28+:**

1. **Default to region-scoped reads.** Use `head` / `tail` / `view_range` for lesson HTML, precedent lesson HTML, and precedent SVG — not full-file reads. Build chats were consuming 30–50% more input tokens than necessary. Full-file reads are a deliberate escalation, not the default.
2. **Keep `status.md` chat-N sections concise (~1.5 KB target, not 7 KB).** Disk artifacts are the source of truth; the narrative is annotation. Trim ruthlessly: SVG details visible from the SVG header comment don't need to be repeated; healthy getBBox margins are just "clean."
3. **Intermediate catalog status `built_unwired_chat_N`** between SVG-on-disk and wire-complete. Immediately after the SVG file is written, flip the lesson's catalog entry to `built_unwired_chat_N`. The dominant failure mode (5/5 interrupted-artifact cases) hits exactly the SVG→wire window; this records the build automatically. Effective chat 29+ (next build chat).
4. **Slim chat-N+1 kickoff prompt to ~500 bytes** for build chats; modestly longer for render-check chats (visual-gate content list needs explicit listing). Kickoff structure: project root + UNC reminder + 1 sentence pointing to `session.md` + 1 sentence describing the chat-N task. Working files carry the convention details.

Full rationale and pattern recognition in `status.md` → *Phase 7 progress (chat 27)* → "Conventions added this chat" paragraph + the durable bullets in the *Conventions / things to remember* section.

---

## Chat 42 — IN PROGRESS — Phase 8 question-content fill (cohort-2 finish: sprite_* module, 2 lessons + cohort-3 stage)

**Chat 42 plan = finish cohort-2 carry-forward (2 sprite_* lessons: `sprite_sheets.html`, `sprite_groups_layers.html`) + stage chat 43 cohort 3.** Each catalog entry transitions `quiz_drafted` → `done` with `completed_chat: 42` + filled `exercise_card_objective` + 3-element `quiz_topics`. Four-step cohort-finish flow: (1) M0 chat-start stubs validation; (2) M1 `sprite_sheets.html`; (3) M2 `sprite_groups_layers.html` — **sprite_* module 4/4 done at this point, second complete module in Phase 8** after pygame_basics; (4) M3 stage chat 43 (cohort-3 candidate; pick at M3 based on catalog state — likely `game_mathematics_*` module 5 lessons OR a remaining mid-course module) + chat close. Reference pattern: chat-38 pilot + chat-40 cohort 1 + chat-41 cohort 2 partial M1+M2 (rhythm validated across 7 question-fill lessons total to date).

**Read `support/session.md` first** then `support/next-chat-prompt.md` (chat-42 brief) then this todo.md chat-42 section. The chat-41 close paragraphs in `status.md` carry the relevant context: question-fill rhythm holding (M1+M2 ran identically to cohort 1 across 2 lessons before interruption); Finding 9 full-stem h2 ids hold uniformly across all 4 sprite_* lessons per chat-41 M0 spot-check (no per-lesson stem variation in cohort 2); no new findings logged in chat 41.

### Carry-forward disciplines (chat 23/27/30/37/38/39/40 — all active)

Same as chat 40 + chat 41 (no new disciplines this chat — the chat-38 tool-routing lesson + all chat-23/27/30 conventions + the chat-40 question-fill rhythm carry forward unchanged): save-first (chat-23, 41-step chain at chat-41 close → target 44 at chat-42 close), session.md as resume pointer (chat-23), region-scoped reads (chat-27), concise status.md ~1.5 KB per chat-N (chat-27), slim kickoff (chat-27), streaming-narrative per-milestone (chat-30; chat 42 = **13th test bed**, third question-fill chat), tool-routing — `Filesystem:edit_file` for existing files / `Filesystem:write_file` for new files (chat-38 M4), button-attribute shape `data-correct` / `data-hint` / `data-explanation` (chat-40).

### Pre-fill checklist for chat 42

1. Read `support/session.md` first (it'll point at "chat 41 closed at M2 done; chat 42 = cohort-2 finish (2 sprite_* lessons) + cohort-3 stage").
2. Read `support/next-chat-prompt.md` (chat-42 brief).
3. Validate chat-41-close pre-stage (M0): `support/phase8-pedagogy-catalog.json` at 7 `done` + 57 `quiz_drafted` + 0 `not_started`; this `todo.md` chat-42 section is present; `status.md` has a chat-42 placeholder section in cohort-finish shape; `support/session.md` is chat-42-IN-PROGRESS; chat-41 lesson edits unchanged on disk (`sprite_loading_images.html` + `sprite_animation.html` both carry their populated `<section class="lesson-section">` blocks per the catalog `done` state).
4. Per-lesson region-scoped read of lesson body (`head 80–130` + `tail 100–120` typically sufficient — chat 41 M1 used `head 130` + `tail 120`).
5. Per-lesson edit anchor: the entire `<section class="lesson-section">…</section>` block is the `oldText` for `Filesystem:edit_file`; new section keeps the same outer wrapper and same `<h2 id="practice-exercise-build-<stem>">` (full-stem per Finding 9 — `practice-exercise-build-sprite-sheets` and `practice-exercise-build-sprite-groups-layers`).

### Phase-8 working file locations (durable, chats 37/38/39/40/41/42+)

- Catalog: `support/phase8-pedagogy-catalog.json` (chat-42 M1+M2 each flip one entry `quiz_drafted` → `done`).
- JS init port: `js/nav-enhancements.js` (chat 38 M2 — done; chat 42 doesn't touch).
- Quiz CSS port: `styles/enhanced.css` (chat 38 M2 — done; chat 42 doesn't touch).
- Scaffold script: `support/inject_pedagogy_scaffold.py` (chat 39 — done; chat 42 doesn't touch).
- Pilot lesson: `pygame_basics_game_loop.html` (chat 38 done).
- Chat-40 cohort 1 lessons (all `done`): `pygame_basics_drawing.html` / `pygame_basics_input.html` / `pygame_basics_collision.html` / `pygame_basics_sound.html`.
- Chat-41 cohort 2 partial (both `done`): `sprite_loading_images.html`, `sprite_animation.html`.
- Chat-42 cohort-2 finish lessons: `sprite_sheets.html`, `sprite_groups_layers.html` (both at `quiz_drafted` post-chat-41; chat-42 fills real content).

---

## Chat 41 — ✅ closed at M2 done (Phase 8 question-content fill cohort 2, partial — 2 of 4 sprite_* lessons; M3+M4 carry forward to chat 42)

**Chat 41 closed at M0/M1/M2 done; M3+M4+M5 carried forward to chat 42 due to interruption between M2 close and M3 start.** Cohort-2 target was 4 sprite_* lessons; 2 landed (M1 `sprite_loading_images.html` "Cache the Spin, Skip the Tax" — load `star.png` once with `.convert_alpha()` + pre-rotate 36 copies + indexed cache lookup proving the loop pinned at 60 FPS via `clock.get_fps()`; M2 `sprite_animation.html` "Two States, One dt" — Idle 4×200ms / Walk 6×100ms with `dt`-accumulator timing + `change_state` reset semantics + loop=True/False end-of-animation branches) per the chat-41 brief's documented "drop the last lesson … chat-N+1 carries forward" safety-valve, triggered by interruption rather than context-tightening with 2 lessons dropped instead of 1. **No defect** — chat-23 save-first discipline held: M1 + M2 each atomically complete on disk (lesson HTML edit + catalog flip + status.md narrative paragraph all landed) at the moment of interruption. M5-equivalent staging done in the recovery turn (status.md chat-close paragraph + chat-42 placeholder section seeded above + 4 file ops landed cleanly: `status.md` / `support/next-chat-prompt.md` / `support/session.md` / this `todo.md`). **Phase 8 status delta:** 5/64 done + 59/64 quiz_drafted → **7/64 done + 57/64 quiz_drafted**; sprite_* module 2/4 (half-complete; chat 42 finishes the module). **No new Phase-8 findings** — chat-41 starts and closes with the same **9 findings** carried in from chat-40 close (cohort-2 question-fill rhythm ran identically to cohort 1 across the 2 landed lessons; Finding 9's full-stem h2 ids held uniformly across all 4 sprite_* lessons per M0 spot-check; the 2 landed lessons surfaced no new rhythm-or-pattern surprises). **41-step save-first chain** at chat-41 close (chat-25 close = step 1 → chat-41 close = step 41; chat 41 contributed 4 steps M0/M1/M2/Chat-close, falling short of the original target step 43 by 2 steps because M3/M4 didn't run; 6 prior interruption-recoveries + this 7th interruption all preserved chain). **12th consecutive clean exercise of chat-30 streaming-narrative discipline** (chats 30–41 = 12/12 clean — counting chat 41 as clean since M0/M1/M2 paragraphs all landed incrementally pre-interruption per discipline; the "drop the last lesson" carry-forward is a planned shape, not a discipline failure). **MCP outages: zero across chat 41** (3rd consecutive zero-outage chat). **Tool-routing handled cleanly throughout** — `Filesystem:edit_file` for all M1+M2 lesson edits + catalog flips + the chat-close staging edits to status.md and todo.md; `Filesystem:write_file` for the chat-42 stage of next-chat-prompt.md and session.md (both with defensive head re-reads confirming WSL landing); zero `create_file` calls; chat-38 M4 tool-routing lesson honored. **Project transitions to chat 42 = cohort-2 finish (2 sprite_* lessons: sheets + groups_layers) + cohort-3 stage** with the 9 findings + interruption-carry-forward shape in hand. Detailed walkthrough lives in `status.md` → *Phase 8 progress (chat 41 — question-content fill: cohort 2, sprite_* module, 4 lessons — closed at M2 done; M3+M4+M5 carried forward to chat 42)*.

---

## Chat 40 — ✅ closed clean (Phase 8 question-content fill cohort 1 — pygame_basics module 5/5 done)

**Chat 40 closed clean across all 5 milestones + chat close.** First question-fill chat in Phase 8; cohort 1 target (4 pygame_basics lessons) hit precisely as the brief planned with no context-tightening drop-the-last-lesson. **M0** validated chat-39-close 5-file pre-stage clean (33rd save-first step); logged Phase-8 **Finding 9** (cosmetic h2 id stem divergence — pilot uses short stem `practice-exercise-build-game-loop`, scaffold-injected lessons use full stem like `practice-exercise-build-pygame-basics-drawing`; 1 short + 63 full across the cohort; Phase 9 lint candidate). **M1** authored real Practice Exercise + 3-question Quick Quiz for `pygame_basics_drawing.html`: exercise = "A Sun, a Hill, and a House" (filled rect/circle/polygon + non-zero `width` outline scene); Q1 = top-left-origin coordinate system (Y grows down; Common Mistakes #2), Q2 = optional integer `width` arg (0=fill, ≥1=outline), Q3 = `Surface.convert()`/`convert_alpha()` performance (Common Mistakes #5). Catalog flipped `quiz_drafted` → `done` with `completed_chat: 40`. 34th save-first step. **M2** authored real content for `pygame_basics_input.html`: exercise = "Move and Jump — Two Input Styles in One Program" (continuous arrow movement via `pygame.key.get_pressed()` + one-shot spacebar jump via `KEYDOWN`); Q1 = event-based use case (jump/shoot/menu vs movement; Restaurant Analogy + Key Takeaways #1+#2), Q2 = `pygame.key.get_pressed()` return shape (sequence indexed by keycodes), Q3 = KEYDOWN-fires-once vs get_pressed-every-frame asymmetry (foot-gun: jump in get_pressed launches player off-screen). Catalog flip; 35th save-first step. Chat-40 weathered one mid-M2 auto-compaction recovery cleanly — disk verification confirmed M1 catalog flip + lesson HTML edit had landed pre-interruption; M2 picked up at the lesson-read step with no rework. **M3** authored real content for `pygame_basics_collision.html`: exercise = "Coin Collector — AABB Collision in 30 Lines" (pygame.Rect player + 5 coin Rects + arrow-key movement recap from M2 + `player.clamp_ip(screen.get_rect())` + list-comprehension filter `coins = [c for c in coins if not player.colliderect(c)]`); Q1 = why AABB rectangles first (Pro Tip #1 + Security Check Analogy), Q2 = `a.colliderect(b)` returns bool (vs `a.clip(b)` returns Rect, `rect.collidepoint((x,y))` for points), Q3 = smaller-hitbox-than-sprite for game feel (Pro Tip #3, `rect.inflate(-N, -N)` hint). Catalog flip; 36th save-first step. **M4** authored real content for `pygame_basics_sound.html`: exercise = "Sound Board — SFX, Music, and Volume Together" (K_1/K_2/K_3 play three preloaded `pygame.mixer.Sound` objects, K_M toggles `pygame.mixer.music` background track via pause/unpause, per-source `set_volume()` 'music quieter than SFX' Pro Tip, `pygame.mixer.set_num_channels(8)`); Q1 = Sound vs music architectural distinction (in-memory multi-instance vs streamed single-track; Key Takeaway #2), Q2 = preload Sound objects ONCE before loop (disk+decode cost; Key Takeaway #4 + Common Pitfall #1), Q3 = `set_num_channels(N)` channel semantics (parallel SFX voices, oldest stolen when busy). Catalog flip — **pygame_basics module now 5/5 done, first complete module in Phase 8** (game_loop chat 38 + drawing/input/collision/sound chat 40); 37th save-first step. **M5** landed 4 file ops cleanly: `support/next-chat-prompt.md` overwritten as chat-41 brief (cohort-2 sprite_* module); `support/session.md` overwritten as chat-41-IN-PROGRESS; `status.md` updated with M5 paragraph + revised IN PROGRESS → CLOSED CLEAN headline + chat-41 placeholder section seeded above chat-40 in question-fill cohort shape; this `todo.md` updated with Current-state Phase-8 line refresh + chat-41 IN PROGRESS section + this chat-40 closed-clean stub. Tool-routing handled cleanly throughout (no `create_file` calls; chat-38 M4 lesson honored). 38th save-first step. **Phase 8 status delta:** 1/64 done + 63/64 quiz_drafted → **5/64 done + 59/64 quiz_drafted**. **9 Phase-8 findings total** at chat-40 close. **11th consecutive clean exercise of chat-30 streaming-narrative discipline** (chats 30–40 = 11/11 clean across 5 prior interruption-recoveries; chat 40 itself weathered one mid-M2 auto-compaction). **Save-first-compounding chain unbroken at 38 consecutive steps** (chat-25 close = step 1 → chat-40 close = step 38). **MCP outages: zero across chat 40** (extends chat-39's zero-outage baseline; 2 consecutive zero-outage chats). **Project transitions to chat 41 = question-content fill cohort 2 (sprite_* module, 4 lessons — sprite_loading_images / sprite_animation / sprite_sheets / sprite_groups_layers)** with all 9 findings + the chat-40 question-fill rhythm in hand. Detailed walkthrough lives in `status.md` → *Phase 8 progress (chat 40 — question-content fill: cohort 1, pygame_basics module, 4 lessons)*.

---

## Chat 39 — ✅ closed clean (Phase 8 scaffold-script build — 63 lessons injected + Findings 7+8)

**Chat 39 closed clean across all 5 milestones + chat close.** Built `support/inject_pedagogy_scaffold.py` (~290 lines, ~12 KB; idempotent / `--dry-run` / `--no-backup` / `.bak` siblings / color terminal output / summary block; mirrors `support/wrap_canvas_fallbacks.py` shape; two-anchor selector preferring `<h2 id="what-s-next">` and falling back to `<nav class="lesson-nav">`). **M0** validated chat-38-close 5-file pre-stage clean (27th save-first step). **M1** wrote the script via `Filesystem:write_file` after region-scoped reads of `wrap_canvas_fallbacks.py` (script shape) and `pygame_basics_game_loop.html` tail (canonical placeholder template); 28th step. **M2 first pass** dry-run reported 58 would-update + 1 already-injected (pilot, smoke-test of idempotency logic clean) + 5 no-anchor; tail reads on 2 of the 5 outliers confirmed they have `<nav class="lesson-nav">` but no What's Next h2. **Phase-8 Finding 7 logged:** 5 lessons (3 `_python.html` companions + `polish_playtesting.html` + `publishing_updates.html`) are section-end / appendix-style; lesson-nav fallback is the only anchor for them. **Sub-finding:** the original `LESSON_NAV_RE` had a `\b` bug (word-boundary after a non-word `"` doesn't match when followed by a non-word space); silent because the pilot and 58 first-pass matches all used the preferred anchor. Patched the regex (drop the `\b`; closing quote enforces exact match) + inline comment. **M2 verification re-run** confirmed expected counts byte-for-byte: 63 would-update + 1 already-injected + 0 no-anchor + anchors used what-s-next=58 / lesson-nav=5. 29th step. **M3 live run** flipped 63 lessons to `updated` + 1 `already-injected` + 0 errors; 63 `.bak` siblings written; spot-checked `sprite_animation.html` (preferred-anchor) and `polish_playtesting.html` (fallback-anchor) for clean section landing. **Phase-8 Finding 8 logged:** cosmetic insertion artifacts (extra blank line above section / indent mismatch with host body in some lessons / orphaned `<!-- Lesson Navigation -->` comment in 5 fallback cases); all non-rendering Phase 9 lint candidates. 30th step. **M4** landed 5 file ops cleanly: catalog batch flip via single 63-edit `Filesystem:edit_file` call (1 done + 63 quiz_drafted + 0 not_started); next-chat-prompt.md overwritten as chat-40 brief; session.md overwritten as chat-40-IN-PROGRESS; status.md M4 paragraph + chat-40 placeholder section seeded above chat-39; this todo.md updated. Tool-routing handled cleanly throughout (no `create_file` calls; chat-38 M4 lesson honored). 31st step. **Phase 8 status delta:** 1/64 done + 0/64 quiz_drafted → **1/64 done + 63/64 quiz_drafted**. **8 Phase-8 findings total** at chat-39 close (1+2 chat 37, 3+4+5 chat-38 M1+M2, 6 chat-38 M3, 7+8 chat-39 M2+M3). **10th consecutive clean exercise of chat-30 streaming-narrative discipline** (chats 30–39 = 10/10 clean across 4 prior interruption-recoveries; chat 39 itself had zero interruptions). **Save-first-compounding chain unbroken at 32 consecutive steps** (chat-25 close = step 1 → chat-39 close = step 32). **MCP outages: zero across chat 39** (recovers from chat-38's 1 outage; baseline at 1 consecutive zero-outage chat). **Project transitions to chat 40 = question-content fill cohort 1 (pygame_basics module, 4 lessons)** with all 8 findings + tool-routing + scaffold-script + 63-lessons-with-TBD-section state on disk. Detailed walkthrough lives in `status.md` → *Phase 8 progress (chat 39 — scaffold-script build for the 63 remaining lessons)*.

---

## Chat 38 — ✅ closed clean (Phase 8 pilot — pygame_basics_game_loop.html end-to-end)

**Chat 38 closed clean** — pilot lesson `pygame_basics_game_loop.html` validated Phase 8 end-to-end across all 5 milestones (M0 validation / M1 read+author / M2 JS+CSS port / M3 browser-verify / M4 catalog flip + stage chat 39 / Chat close). **M0** validated chat-37-close pre-stage clean (5 files in chat-38 state, 21st save-first-compounding step). **M1** authored Practice Exercise + 3-question Quick Quiz with real questions about game-loop concepts (Q1: `pygame.event.get()` event-pump / OS-responsiveness; Q2: `screen.fill()` vs `pygame.display.flip()` back-buffer / double-buffering; Q3: `clock.tick(60)` frame-rate semantics) inserted between Key Takeaways `</div>` and What's Next? `<h2>` (resolved Open Q1: pedagogical flow lesson body → Takeaways → Practice+Quiz → What's Next? → lesson-nav). Lesson grew 21,608 → 28.38 KB. **Phase-8 Finding 3 logged:** pygame_gamedev lessons routinely have pre-existing inline `Practice Exercise / Try It / Challenge` h2 sections (60/64 per chat-37 audit) — left untouched, used distinct new h2 id `practice-exercise-build-game-loop`, both coexist (informal-mods + formal-write-from-scratch). **M2** ported `initQuizInteractivity()` to `js/nav-enhancements.js` IIFE verbatim from python_intro `js/course-enhancements.js` lines 328–345 (one-line addition to `DOMContentLoaded` init list at line 32 + 19-line markup-contract comment + 17-line function); ported quiz CSS to `styles/enhanced.css` line ~1142 (5.3 KB / 226 lines / 9 named selectors with hardcoded semantic colors, immediately before existing Phase-7 canvas-fallback block); resolved Open Q2 (quiz CSS source = python_intro `styles/main.css` `/* === Quiz Styles === */` block); Findings 4 (python_gamedev's `:root` is a partial subset of python_intro's — missing `--success-bg/border` etc., hardcoded inline colors used as workaround) and 5 (python_gamedev had no `.card`/`.card-info` previously, only `.exercise-card` with different shape) logged. **M3** browser-verified manually by Ray on `localhost:5503` due to recovery-turn MCP outage — all 9 quiz buttons function as designed (selected/correct/incorrect classes apply, `.quiz-feedback` populates with hint/explanation text, no console errors); **Phase-8 Finding 6 logged:** user-driven manual verification is a workable MCP-outage substitute in recovery turns (~3-min roundtrip, equivalent semantic coverage). **M4** flipped `pygame_basics_game_loop.html` catalog `not_started` → `done` with completed_chat=38 + filled `exercise_card_objective` + `quiz_topics` (all from M1 content); staged chat 39 (overwrote `support/next-chat-prompt.md` for scaffold-build, overwrote `session.md` to chat-39-IN-PROGRESS, seeded `status.md` chat-39 placeholder section in scaffold-build shape, updated this `todo.md`). **Phase 8 status delta:** 0/64 → **1/64 done**; 63/64 still `not_started`; chat-39 staged for scaffold-build to flip 63 → `quiz_drafted`. **Two interruptions weathered:** mid-flight auto-compaction between M2 close and M3 start (recovery picked up at M3 with full disk verification, no rework); chat-38-M4 tool-routing error (two `create_file` calls hit Claude's container instead of WSL and silently failed on disk — recovered via `Filesystem:edit_file` full-doc swaps; lesson-learned now codified in chat-39 carry-forward and `support/session.md`). **9th consecutive clean exercise of the chat-30 streaming-narrative discipline** (chats 30–38 = 9/9 clean across 4 interruption-recoveries). **Save-first-compounding chain unbroken at 26 consecutive steps** (chat-25 close = step 1 → chat-38 close = step 26, chat-38 contributing 6 steps M0/M1/M2/M3/M4/Chat-close). **Phase-8-specific Finding 6: user-driven manual browser-verify is a workable MCP-outage substitute in recovery turns.** Detailed walkthrough lives in `status.md` → *Phase 8 progress (chat 38 — pilot lesson end-to-end on `pygame_basics_game_loop.html`)*.

---

## Chat 37 — ✅ closed clean (Phase 8 kickoff: M2 + stage-38; pilot deferred to chat 38)

**Chat 37 closed clean** with milestone-shape adjustment from kickoff template. Five milestones executed (M0 stubs / M1 audit / M2 decisions+catalog / M3 pilot-deferred / M4 stage-chat-38) plus chat close; pilot deferred to chat 38 because audit + decisions + catalog seed left insufficient context for a fresh end-to-end pilot. **M0** validated chat-36-close pre-stage clean (all 4 files in chat-37 state). **M1** delivered six findings: python_intro `lesson_01.html` lines 354–448 confirms exact Practice Exercise + Quick Quiz markup; `data-correct` / `data-hint` / `data-explanation` button-attribute shape confirmed; `initQuizInteractivity()` (17 lines, self-contained) located at python_intro `js/course-enhancements.js` lines 328–345; python_gamedev `js/course-enhancements.js` (346 lines, Prism-heavy) lacks any quiz code; python_gamedev `js/nav-enhancements.js` (254 lines, IIFE+named-functions, all 64 lessons already include it) is the cleaner port target; 64-lesson `has_quiz` audit = 0/64, **clean slate**; pilot lesson confirmed = `pygame_basics_game_loop.html` (canonical Lesson 1.1 per `index.html`). **M2** landed three decisions + catalog: scaffold-script approach (rationale: 0/64 starting state + mechanical markup + value-add is questions not markup); JS port target = `nav-enhancements.js` (one-line + one-function); quiz CSS port surfaces as chat-38 M2 work targeting `styles/enhanced.css`; **`support/phase8-pedagogy-catalog.json` written at 12,718 bytes with 64 entries at `not_started` across 13 modules** (genres 9, others 5, sprites 4, effects 1). **M3** documented the milestone-shape adjustment (kickoff template assumed pilot-in-chat-37; remaining context after M2 wouldn't safely fit pilot + close). **M4** wrote `support/next-chat-prompt.md` for chat 38 (pilot brief), updated this `todo.md` (flip chat 37 → closed-clean stub, add chat 38 IN PROGRESS section, update Current-state Phase 8 line), seeded `status.md` chat-38 placeholder section in pilot-shape (M0/M1/M2/M3/M4/Chat close = stubs/read+author/JS+CSS port/browser-verify/catalog flip+stage-39/close), overwrote `session.md` as chat-38-IN-PROGRESS. **Phase 8 status delta:** ⏳ → 🔄 kickoff-done; 0/64 lessons done; 1/64 staged for chat 38 pilot. **8th consecutive clean exercise of the chat-30 streaming-narrative discipline** (first non-Phase-7 application). **Save-first-compounding chain unbroken at 20 consecutive chats** (chat-25 close → chat-37 close, surviving four prior interruption events). **Phase-8-specific Finding 1: Phase-8 kickoff chats are heavier than Phase-7 build chats** (audit + decisions + catalog seed » single-SVG-build); the natural Phase-8 rhythm is closer to *decisions chat / pilot chat / scaffold-build chat / question-fill chats* than Phase-7's *build / render-check* alternation. Detailed walkthrough lives in `status.md` → *Phase 8 progress (chat 37 — kickoff: audit + pilot lesson — first non-Phase-7 chat)*.

---

## Chat 36 — ✅ closed clean (FINAL Phase 7 chat — PHASE 7 CLOSED)

**Chat 36 closed clean — PHASE 7 CLOSED at 15/15 wired-AND-render-checked clean + 0 wired-but-render-pending + 0/15 deferred.** Render-checked SVG #31 `tower-defense-lane.svg` in `genres_tower_defense.html` at iframe-simulated 380×820 across the standard 7-milestone pipeline. **All 5 verification points pass.** **11th confirmation of the chat-15 `!important` hardening logged.** **Second two-pattern hardening case** after chat-31 racing (canvas inline `style="border: 2px solid #333; display: inline-block; background: #2d4a2b;"` carries BOTH `display: inline-block` AND `background:` patterns; `enhanced.css` rule wins on specificity, computed display is `none`). All four getBBox tightest margins byte-identical to chat-35 m3 post-fix to two decimals (right 59.72 / bottom 11.92 / left 14.00 / top 10.05 px) — zero SVG drift between chat-35 build and chat-36 render-check. Cold-fetch 311.9 ms strongly validates the chat-33 ~315 ms baseline as a **5th data point** (5-point spread 309.5–319.1 ms, all within 5 ms of median). Visual gate verified all 9 structural checks via the chat-26/30/32/34 inline-render workaround: header band exactly "TOWER DEFENSE LANE — TOWER COVERAGE & ENEMY ROUTING"; 4 range circles correctly bucketed by radius (Basic r=68 amber, Cannon r=58 red-orange + dashed splash AOE inner ring r=30, Slow r=64 cyan, Laser r=78 indigo as visually largest); 5 tower glyphs (🏹 💣 ❄️ ⚡ 💰) including Bank "+5 g/s" income tag and "range 0" caption ending; 3 path enemies (Fast r=6, Basic r=8, Tank r=11 at expected (388, 360) with HP-bar nub rect); 8-waypoint path with 8 segments + 7 chevrons; SPAWN + BASE markers; legend with TOWERS + ENEMIES headings + "Circles = tower coverage" footnote + all 5 costs ($100/$200/$300/$150/$250) + Flying detailed callout (late waves + only Laser hits) + Boss detailed callout (hp 1000 + wave 10+); footer caption at y=426 (post-chat-35-m3 symmetric fix preserved on disk); olive-green lane background. chat-34 sticky-toc-collapse discipline applied (1 `details.toc-card` collapsed before visual gate). **7th consecutive clean exercise of the chat-30 streaming-narrative discipline** (chats 30–36 = 7/7 clean even counting three interruption-recoveries at chats 33/34/35). **Save-first-compounding chain unbroken at 17 consecutive chats** (chat-25 close → chat-36 close, surviving four interruptions: chat-29 auto-compaction + chat-33 mid-flight + chat-34 screen-reset + chat-35-attempt-1 screen-reset). **Phase 7 status delta:** 14/15 → **15/15 wired-AND-render-checked clean**; 0 wired-but-render-pending; **0/15 deferred**; **PHASE 7 CLOSED**. Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 36 — SVG #31 render-check: `tower-defense-lane.svg` in `genres_tower_defense.html` — FINAL Phase 7 chat)*.

---

## Chat 35 — ✅ closed clean (across two attempts; third consecutive recovery exercise)

**Chat 35 closed clean across two attempts** — built and wired SVG #31 `tower-defense-lane.svg` for `genres_tower_defense.html` (the LAST deferred Phase 7 SVG). Chat-35-attempt-1 hit a screen reset between milestones 4 and 5 (chat-23 save-first discipline preserved every artifact: SVG on disk + lesson wired + catalog at `built chat 35` + status.md M1–M4 narrative + session.md milestone-4-complete pointer; only M0/M5/Chat-close paragraphs in status.md were template stubs and the four M5 file-pre-stages were not yet started); chat-35-attempt-2 verified disk state and resumed at milestone 5 cleanly. **Third consecutive recovery exercise** after chat-33 (mid-flight m3↔m4) and chat-34 (screen reset after m0/m1/m4); the chat-23 disciplines (save-first-not-last + `support/session.md` as resume pointer + chat-30 streaming-narrative on disk) made the recovery essentially mechanical. Two-region diagram at viewBox 720×440 (chat-33 RPG dim chosen over catalog 720×400 for legend headroom): top-down lane (4 range circles + Bank no-circle + 3 path enemies + 8-waypoint S-curve over olive-green field echoing the lesson's `#2d4a2b` canvas backdrop), legend column (5 towers + 3 path enemies + Flying-laser-only + Boss-wave-10+ callouts), centered footer caption. Chat-31-style **preventive** symmetric fix in m3 (footer y=432→426 for tightest-bottom 5.92→11.92 px clearance — **first preventive use** of the discipline; previous uses were reactive). Catalog flipped to `built chat 35` with **fully rewritten** 5-tower fallback_alt + fallback_caption (catalog had described 3 towers; SVG has 5 including the Bank economy tower with +5g/s income tag and range 0). **Phase 6 alt 1797 chars** (longest Phase-7 alt to date — between chat-31 racing 1324 and chat-33 RPG 1843; justified by 5 distinct tower types + 3 path enemies + 2 legend-only callouts + splash AOE detail). **Phase 7 status delta:** 14/15 wired-AND-render-checked unchanged; +1 wired-but-render-pending = chat-35 SVG; **deferred drops 1/15 → 0/15 — LAST Phase 7 build chat.** **6th consecutive clean exercise of the chat-30 streaming-narrative discipline** (chats 30–35 = 6/6 clean even counting three interruption-recoveries). **Save-first-compounding chain unbroken at 16 consecutive chats** (chat-25 close → chat-35 close, surviving chat-29 auto-compaction + chat-33 mid-flight + chat-34 screen-reset + chat-35 screen-reset). **Third genuine exercise of the chat-27 `built_unwired_chat_N` intermediate catalog status** (chat 31 first NORMAL flow, chat 33 first INTERRUPTION-RECOVERY success, chat 35 third NORMAL flow). Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 35 — SVG #31 build: `tower-defense-lane.svg` for `genres_tower_defense.html`)*.

---

## Chat 34 — ✅ closed clean (across two attempts; second consecutive recovery exercise)

**Chat 34 closed clean across two attempts** — render-checked SVG #30 `rpg-stats-progression.svg` in `genres_rpg.html` at iframe-simulated 380×820. **All 5 verification points pass; 10th confirmation of the chat-15 `!important` hardening** (canvas inline `style="border: 1px solid #333; display: inline-block;"` — display-only inline pattern, simpler than chat-31 racing's two-pattern case which had both `display: inline-block` AND `background: #1a1a1a`). All four getBBox tightest margins byte-identical to chat-33 build-time (right 52.02 / bottom 14.4 / left 21.48 / top 12.05 px); cold-fetch 315.9 ms strongly validates chat-33's ~315 ms baseline (firmly settled now: cold-fetch is fetch-bound, not bbox-walk-bound). **Chat 34 was itself a recovery exercise**: chat-34-attempt-1 got through milestones 0 / 1 / 4 before a screen reset returned to the chat-33 kickoff prompt; chat-34-attempt-2 verified disk state (3 of 4 chat-33-close pre-staged files in chat-34 state, plus the chat-33 status.md tail was backfilled by chat-34-attempt-1's milestone 0), re-ran milestones 2 + 3 fresh (10th hardening confirmation properly logged on-the-record), trusted milestone 4's on-disk paragraph (already complete and detailed), then milestone 5 + chat close. **Fifth consecutive clean exercise of the chat-30 streaming-narrative discipline** (chats 30 / 31 / 32 / 33 / 34 = 5/5 clean — counting chat-33 and chat-34 as clean despite their respective interruption-recoveries since both recoveries closed clean). Chat-32 Live-Server wrinkle reproduced exactly: m2 status.md save reloaded the parent host page (status.md sits in served root) which blew away the iframe between m2 and m3; mitigation was to re-inject the iframe and capture m3 structural data BEFORE writing m3 narrative. Phase 7 status delta: 13/15 → **14/15 wired-AND-render-checked clean**; 0 wired-but-render-pending; deferred 1/15 unchanged (`genres_tower_defense` only). Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 34 — SVG #30 render-check: rpg-stats-progression.svg in genres_rpg.html)*.

---

## Chat 33 — ✅ closed clean

**Chat 33 closed clean across two attempts** — built and wired SVG #30 `rpg-stats-progression.svg` for `genres_rpg.html`. Chat-33-attempt-1 interrupted between milestone 3 (getBBox pre-clear, complete) and milestone 4 (lesson HTML edits + catalog finalize); chat-33-attempt-2 verified disk state (SVG on disk matches milestone-2 narrative, catalog at `built_unwired_chat_33`, lesson unwired) and resumed at milestone 4. **First time the chat-27 `built_unwired_chat_N` convention paid off as an interruption-recovery signal** — chat 31 was the first NORMAL exercise (clean within one chat); this is the first RECOVERY exercise. **Three-column character-sheet panel** at viewBox 720×440: STATS & VITALS (4-stat attributes STR/INT/DEX/VIT with indigo equipment bonuses, 3 semantic-colored vitals bars HP/MP/Stamina, amber XP bar Lvl 5→6, Warrior class), INVENTORY (4×4 representative slice of the 40-slot grid with 3 filled items, Carried legend, gold counter), EQUIPMENT (3 paper-doll slots — Weapon Iron Sword +5 ATK/+2 STR, Armor Leather +3 DEF/+1 VIT, Accessory empty). **Two new chat-33 findings:** (1) transform-text false positive in the getBBox walker — text inside `<g transform="...">` returns LOCAL coords, fix is to move text out; (2) cold-fetch timing context revises chat-32's rAF-removal magnitude — chat-33 with no rAF + active foreground tab measures ~315 ms cold-fetch; chat-32's 4.6 ms was likely hot-cache. rAF removal still the right convention. Phase 7 status delta: 13/15 wired-AND-render-checked unchanged; +1 wired-but-render-pending = chat-33 SVG; deferred drops 2/15 → 1/15 (`genres_tower_defense` only). Streaming-narrative discipline held across all 6 milestones — fourth consecutive clean exercise (chat 30 + 31 + 32 + 33 = 4/4 clean even counting the chat-33 mid-flight interruption, since recovery was clean). Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 33 — SVG #30 build: `rpg-stats-progression.svg` for `genres_rpg.html`)*.

---

## Chat 32 — ✅ closed clean

**Chat 32 closed clean** — render-check SVG #29 `racing-track-physics.svg` in `genres_racing.html` was completed at iframe-simulated 380×820. All 5 verification points passed. **9th confirmation of the chat-15 `!important` hardening** (canvas inline `style="border: 2px solid #333; display: inline-block; background: #1a1a1a;"` — BOTH `display: inline-block` AND `background: #1a1a1a` patterns present; `enhanced.css` `!important` rule wins, computed display "none"). getBBox re-clear: 25 text/tspan nodes, 0 overflows, all four tightest margins byte-identical to chat-31 build-time (right 5.11 / bottom 15.4 / left 21.02 / top 11.05 px) — confirms no SVG drift. **chat-31 rAF-removal finding strongly validated** — elapsed 4.6 ms vs chat-31's 315.2 ms (68× speedup) and chat-30's 123.8 ms (27×); rAF was the bottleneck, not the bbox walk. Settled convention going forward. Streaming-narrative discipline held across all 6 milestones (Milestone 0 / 1 / 2 / 3 / 4 / 5 + Chat close), no interruption — third consecutive clean exercise (chat 30 + 31 + 32 = 3/3). Phase 7 status delta: 12/15 → **13/15 wired-AND-render-checked clean**; 0 wired-but-render-pending; deferred drops to 2/15 (`genres_rpg` / `genres_tower_defense` remaining). Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 32 — SVG #29 render-check: `racing-track-physics.svg` in `genres_racing.html`)*.

---

## Chat 31 — ✅ closed clean

**Chat 31 closed clean** — built and wired SVG #29 `racing-track-physics.svg` for `genres_racing.html` in one build chat (deferred → wired). Two-panel diagram: top-down track view with car-at-apex showing 3 labeled vectors (velocity / grip max / required lateral) plus a dashed skid path; friction-circle inset with cross axes and a saturated-plus-overshoot vector pair geometrically tied to panel 1. **First genuine exercise of the chat-27 `built_unwired_chat_N` intermediate catalog status** — flow `needs_new_svg → built_unwired_chat_31 → built chat 31` ran end-to-end with the SVG-on-disk-then-flip step happening before wiring. **First build-chat application of the chat-30 streaming-narrative discipline** — 5 milestone paragraphs appended incrementally to `status.md`, no interruption. getBBox pre-clear caught 1 overflow ("Turn R" axis label by 0.89 px); fixed via 2-edit symmetric label shift; re-run clean (0 overflows, tightest right margin 5.11 px). New chat-31 finding: do NOT use defensive `requestAnimationFrame()` waits before getBBox (rAF stalls on backgrounded tabs; getBBox triggers layout itself). Phase 7 status delta: 12/15 wired-AND-render-checked unchanged; +1 wired-but-render-pending = chat-31 SVG; deferred drops to 2/15. Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 31 — SVG #29 build: `racing-track-physics.svg` for `genres_racing.html`)*.

---

## Chat 30 — ✅ closed clean

**Chat 30 closed clean** — render-check SVG #28 `playtesting-feedback-loop.svg` in `polish_playtesting.html` was completed at iframe-simulated 380×820. All 5 verification points passed; 8th confirmation of the chat-15 `!important` hardening; chat-30 streaming-narrative discipline successfully exercised end-to-end (6 milestone paragraphs appended incrementally to `status.md` as each step completed; chat-close write was just a short wrap-up). Phase 7 wired-AND-render-checked count went from 11/15 to 12/15. Deferred unchanged at 3/15 (`genres_racing` / `genres_rpg` / `genres_tower_defense`). Detailed walkthrough lives in `status.md` → *Phase 7 progress (chat 30 — SVG #28 render-check)*.

---

## Out-of-scope flags from chat 15 (decide separately if any are worth a focused chat)

1. **`details.toc-card` floating outline overlays canvas-fallback area on mobile.** Sticky `<details class="card toc-card auto-toc">`, z-index 100, ~229 px tall, default-open. Visually covers canvas-fallback figure on phones. Pre-existing (Phase 5 auto-TOC infra). Possible fixes: default-collapsed below 768 px, fully hidden below 768 px, or shrink the sticky height.
2. **Canvas demo JS animation loops keep running on mobile when canvas is hidden** — wasted CPU. Pre-existing across all 53 canvas demos. Easy per-lesson fix (`if (matchMedia('(max-width: 768px)').matches) return;` early-out at top of animation loop) but rolling out to 53 canvases is a script-pass.
3. **Lazy-load on canvas-fallback `<img>`** is correct behavior — noted only because it caused initial diagnostic confusion (`naturalWidth: 0` on first JS query, resolves on scroll). Implication for future render checks: query `naturalWidth` AFTER scrolling into viewport.

---

## Live open questions (full list incl. resolved in `status.md`)

1. `_python.html` companion files — standalone learning resources or appendix code dumps? (3 still missing both Summary + Exercise as of post-Phase-6 audit.)
2. `index.html` — join the design system (CSS vars, theme toggle) or stay bespoke?
