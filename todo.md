# Python Game Dev — Course Modernization TODO

> Forward-looking next-chat steering. **Read `support/session.md` first** for the immediate resume pointer; then this file for the next-chat task. Phase-1-through-Phase-8 history lives in per-chat archives at `support/archive/chat-NN.md` (chats 64+) or in this file's git history (chats 30-63). Phase-9 catalog of record: `support/phase9-lint-catalog.json`.

**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\python_gamedev`
(Use `\\wsl$\` paths, not `\\wsl.localhost\`.)

---

## Current state

- **Phases 1–7:** ✅ done. Phase 7 closed at chat 36 (15/15 lessons wired + render-checked clean). 25 Phase-6 SVGs + 16 Phase-7 SVGs shipped clean.
- **Phase 8:** ✅ **CLOSED at chat-84.** 64/64 lessons `done` across 13 modules; 21 per-chat archives (`chat-64.md` through `chat-84.md`); 14 Phase-8 findings + Finding-14 codified at chat-54 (recovery mechanism, ultimately invoked 19+ times across Phases 8+9). Historical detail: `support/archive/chat-84.md` (Phase 8 final archive).
- **Phase 9:** 🔄 **in-progress at chat-120 M2 close** (chat-116 = Phase-9 lint cohort 26 on `ai_decision_making.html` 153-line opening ai module 1/5 → **2/5 partial**; 23 atomic Decision-2 pairs across Blocks 1-2 via two Filesystem:edit_file calls (MEDIUM-bucket split per chat-105 PRECEDENT into 3 pairs Block 1 first call + 20 pairs Block 2 second call); both calls dry-run-then-commit clean first-try; Block 1 UtilityAI behavior-rich class shape ONE-class 10+ methods (decide + setup_actions + score_eat/fight/flee/explore + get_personality_modifier + response curves linear/quadratic/exponential/sigmoid/gaussian); Decision 1 PASS-with-zero-conversions (TWENTY-FIFTH consecutive); Decision 3 NO-SKIP at 153 max-block well below 300-line monolithic threshold (monolithic streak remains PAUSED at 15 from chat-111); **NEW chat-116 MICRO-PRECEDENT: in-place typing-import bump** for blocks with existing typing line (Block 1 typing import `Dict, List, Tuple, Optional` bumped in-place to add Any alphabetized to Any, Dict, List, Optional, Tuple yielding ZERO line-count growth, contrast with chat-104 from-scratch +1 line growth case); **NEW chat-116 MICRO-PRECEDENT secondary: zero-labeled-blocks regex-broadening case** extends chat-114 catalog-vs-regex python_block_count reconciliation discipline to ZERO-labeled case (this lesson used class-on-pre form for Block 1 and bare-pre-code form for Block 2; labeled-on-code regex returned ZERO hits; broadened regex correctly identified both blocks); **NEW chat-116 PRECEDENT: MEDIUM-bucket 3+20 asymmetric split-call viable** for blocks with heavily imbalanced pre-existing annotation coverage (Block 1 needed only 3 pairs due to extensive pre-existing annotations; Block 2 needed 20 pairs as essentially-blank starting state); **chat-116 SCOPE-CLARIFICATION on chat-115 tuple-unpack-must-split**: applies to INSTANCE-ATTRIBUTE multi-target unpacks; LOCAL-SCOPE tuple unpacks within a def `dx, dy = f[0]-a.pos[0], f[1]-a.pos[1]` fall under chat-106 locals-optional clause; chat-104 axis-bump rule confirmed in-place-bump case: Block 1 typing-import bumped in-place so example_solution_size_lines stays 153 (no line-count growth from in-place edit); Block 2 grew 143 → 144 from new-line typing import addition but NOT largest so example_solution_size_lines unchanged BUT practice_exercise_solution_size axis bumped 143 → 144 since Block 2 IS the practice-exercise-tracked block; TWO-PHASE catalog flip pattern reused per chat-110+111+112+113+114+115 PRECEDENT: Phase A 4 surgical edits (consolidated entry status+coverage + 2 stats decrements/inserts + 1 by_type_hint_coverage migration ~0.7 KB) + Phase B notes-embed single pair (~5.8 KB em-dash-free); both phases committed clean first-try; catalog 324,530 → 330,397 bytes (delta +5,867); AI module 1/5 → **2/5 partial**. **21/24+ high-priority lessons linted; cumulative 27 lessons linted = 21 high-priority + 6 medium-priority/catalog-cleanup closeouts.** [Chat-115 and earlier detail preserved in per-chat archives at `support/archive/chat-NN.md`; chat-116 detail at `support/archive/chat-116.md`.]
  - **8/12 Phase-9 modules complete:** architecture 5/5 (closed chat-92) + polish 5/5 (closed chat-100) + networking 4/5 high-priority (closed chat-105; only medium networking_client_server remains) + game_mathematics 1/1 (closed chat-106) + physics 3/3 (closed chat-109) + sprites 2/2 (closed chat-111) + graphics 5/5 (closed chat-115) + ai 5/5 (closed chat-120).
  - **Module partials:** none (chat-120 closed ai 4/5 → 5/5 = EIGHTH complete Phase-9 module). Remaining open: genres 0/4 high-priority (chat-121 PIVOT mandatory; primary target genres_rpg_python.html 462).
  - **Locked decisions** (chat-86 M3): (1) pragmatic dataclass split; (2) type-hint mid-tier; (3) size triage with comprehensive-API-surface SKIP vs game-framework-demo DISTILL.
  - **Locked process** (chat-100 cutover + chat-102 automation cutover + chat-105 per-block-group split-call cutover + chat-106 split-strategy forward-ref precedent + chat-107 MEDIUM-bucket split-call calibration plus full-body-rewrite __init__ disambiguation + chat-108 catalog-flip-in-chat-via-local-script-execution precedent + chat-109 push-final-catalog-back-via-Filesystem-edit-batch cutover + chat-110 edit_file-19KB-payload-viable PRECEDENT + chat-111 33-pair-LARGE-split-20-13 PRECEDENT): catalog notes ~2-22KB calibration band, full-collapse at M2, archives ~3-5KB, briefs ~2KB, session.md ~600B, catalog M1 updates via `support/phase9_catalog_update.py` (replaces Finding-19 split-payload protocol); per-block-group dry-run-plus-commit pairs for LARGE sweeps (greater-than-30 atomic pairs) per chat-105 precedent and for MEDIUM sweeps with oldText uniqueness concerns per chat-107 precedent; split-strategy forward-refs for central-class-library plus auxiliary-block lessons per chat-106 precedent; Decision-2 locals-optional clause exempts no-def-no-class blocks per chat-106 precedent; full-body-rewrite for __init__ methods plus docstring-anchor or inline-comment-anchor for collision-prone single-line method signatures per chat-107 precedent; catalog-flip-in-chat via local-script-execution when Claude environment has bash+Python plus Filesystem MCP read access per chat-108 precedent (`Any` for duck-typed object parameters when no concrete class exists in scope; pygame.math.Vector2 for explicit-Vector2 params even when pygame import missing).
- **Phase-9 cohort sequence (chats 87+):** chat-87 save_load, chat-89 event_systems, chat-90 component_systems, chat-91 state_machines, chat-92 scene_management (architecture complete), chat-93 ai_pathfinding, chat-94 networking_lag_compensation, chat-95 graphics_procedural, chat-96 polish_difficulty, chat-97 polish_tweening, chat-98 polish_sound, chat-99 polish_screenshake, chat-100 polish_playtesting (polish complete), chat-103 networking_basics, chat-104 networking_sockets, chat-105 networking_lobby (networking high-priority complete), chat-106 game_mathematics_vectors (game_mathematics complete), chat-107 physics_bounce_friction (physics opens 0/3 → 1/3 partial), chat-108 physics_collision_response (physics 1/3 → 2/3 partial), chat-109 physics_gravity (catalog-flip-only side-quest closing physics 3/3), chat-110 sprite_animation (opens sprites 0/2 → 1/2 partial), chat-111 sprite_groups_layers (closes sprites 1/2 → 2/2 = SIXTH complete Phase-9 module), chat-112 graphics_shaders (advances graphics 1/5 → 2/5 partial), chat-113 graphics_postprocessing (advances graphics 2/5 → 3/5 partial), chat-114 graphics_lighting (advances graphics 3/5 → 4/5 partial), chat-115 graphics_ui_hud (closes graphics 4/5 → 5/5 = SEVENTH complete Phase-9 module), chat-116 ai_decision_making (opens ai 1/5 → 2/5 partial), chat-117 ai_flocking (advances ai 2/5 → 3/5 partial), chat-118 ai_behavior_trees (advances ai 3/5 → 4/5 partial), chat-119 ai_state_machines M0+scope-only-deferral (no lint pass executed; 17-pair plan frozen for chat-120 execution), chat-120 ai_state_machines (closes ai 4/5 → 5/5 = EIGHTH complete Phase-9 module).
- **4 high-priority lessons remain** across genres (4). sprites module closed at 2/2 after chat-111 sprite_groups_layers; graphics module closed at 5/5 after chat-115 graphics_ui_hud; ai module closed at 5/5 after chat-120 ai_state_machines (EIGHTH complete Phase-9 module). Chat-121 SOFT-DEFAULT mandatory PIVOT: genres bucket `genres_rpg_python.html` 462-line (opens genres 0/4 → 1/4 high-priority; LARGE-bucket multi-call split needed; partial+partial axes; partial-dataclass shape unlike pure-absent ai/physics/sprites/graphics patterns may break TWENTY-EIGHTH consecutive Decision-1-PASS streak; monolithic SKIP streak may RESUME if Block-N crosses 300 on disk despite catalog axis 462). Larger genres alternatives: `genres_puzzle_python` 801, `genres_strategy_python` 911, `genres_tower_defense_python` 1046.

---

## Chat 121 — 🔄 IN PROGRESS — Phase-9 lint cohort 30 (PIVOT to genres bucket mandatory after chat-120 closed ai 5/5; SOFT-DEFAULT `genres_rpg_python.html` 462-line opens genres 0/4 → 1/4 high-priority)

PIVOT to genres bucket mandatory: with ai closed at 5/5 after chat-120, genres is the only remaining high-priority module surface (4 high-priority entries). Primary target: `genres_rpg_python.html` 462-line (opens **genres 0/4 → 1/4 high-priority**; LARGE-bucket multi-call split needed; partial+partial axes; partial-dataclass shape unlike pure-absent ai/physics/sprites/graphics patterns). All 4 genres entries audited as `dataclass_usage=partial` so Decision-1 dataclass-split judgment calls expected; may break TWENTY-EIGHTH consecutive Decision-1-PASS streak. Monolithic SKIP streak may RESUME at chat-121 if genres_rpg_python Block-N surfaces above 300-line threshold on disk despite catalog axis 462 likely indicating a single LARGE Block 1. Larger genres alternatives if Ray prefers: `genres_puzzle_python` 801 / `genres_strategy_python` 911 / `genres_tower_defense_python` 1046. Chat-121 M0 standard chat-109 NEW DISCIPLINE verification: confirm catalog has `linted_chat_120: 1` + ai_state_machines flipped to linted_chat_120; broader sanity check on 3 prior-chat linted entries (recommend ai_state_machines chat-120 + ai_behavior_trees chat-118 + ai_flocking chat-117 most-recent ai-module entries); chat-114 + chat-116 broadened regex `<pre[^>]*><code[^>]*>(.*?)</code></pre>` for disk-vs-catalog python_block_count reconciliation on genres_rpg_python.html. Target + plan in `support/next-chat-prompt.md`.

---

## Chat 120 — ✅ closed at M2 (Phase-9 lint cohort 29; SOFT-DEFAULT ai_state_machines.html confirmed clean at M0; closes ai 4/5 → 5/5 = EIGHTH complete Phase-9 module)

`ai_state_machines.html` linted (Decision 3 NO-SKIP at 299 max-block sitting 1 line below 300-line monolithic threshold per chat-112 PRECEDENT-EXTENSION threshold-not-near-threshold rule; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111 = NO-RESUME at chat-120; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-EIGHTH consecutive matching chat-104/106/107/108/110/111/112/113/114/115/116/117/118 shape with 1 NPCState enum + 7 classes Block 1 (State + IdleState + PatrolState + AlertState + ChaseState + StateMachine + NPC) and 1 NPCState enum + 8 classes Block 2 (State + IdleState + PatrolState + AlertState + ChaseState + FleeState + StateMachine + NPC) all retaining manual __init__ since behavior-rich with enter-execute-exit method scope; Decision 2 MEDIUM sweep at 17 atomic edit pairs across Blocks 1-2 via two Filesystem:edit_file calls dry-run-then-commit clean first-try with zero anchor failures (MEDIUM-bucket near-symmetric 8+9 split per chat-105 PRECEDENT extending chat-118 MEDIUM-bucket 14+14 symmetric pattern to smaller-pair-count variant when both blocks have fewer top-level constructs; chat-120 NEW PRECEDENT 8+9 near-symmetric split balances per-call payload sizes when both blocks have moderate lint surface); Block 1 8 pairs (typing-import-in-place-bump per chat-116 PRECEDENT existing `from typing import Dict, Optional, Any` bumped alphabetized to `Any, Dict, List, Optional` no line growth + 7 per-class atomic full-body rewrites with class-declaration-line natural disambiguation per chat-118 per-class scope discipline); Block 2 9 pairs (from-scratch typing-import per chat-104 PRECEDENT new `from typing import Optional` line +1 + 8 per-class atomic full-class rewrites mirroring Block 1 same-named-class annotation shapes with chat-115 semicolon-multi-statement inline annotations + FleeState Block-2-only state subclass for coward personality); ~17 inline-annotation insertions plus 1 typing-import addition across both blocks). NEW chat-120 MICRO-PRECEDENT-CONFIRMATIONS: (a) MEDIUM-bucket near-symmetric 8+9 split extending chat-118 MEDIUM-bucket 14+14 symmetric pattern to smaller-pair-count variant when both blocks have fewer top-level constructs (7 classes plus 1 enum vs chat-118 Block 1 13 classes); (b) forward-ref-string for class-defined-later-in-same-block annotation reversing chat-118 Blackboard pattern direction (chat-118 Blackboard was defined-earlier-in-same-block-so-unquoted-backward-ref; chat-120 NPC is defined-later-in-same-block-so-forward-ref-string-quoted; both intra-block class reference directions now confirmed working); (c) mixed-typing-import-discipline within one lesson per chat-118 PRECEDENT-CONFIRMATION (Block 1 in-place-bump per chat-116, Block 2 from-scratch per chat-104) NOW SOFT-DEFAULT third consecutive chat-117-118-120 confirmation for ai-module mixed-typing-import lessons; (d) SOFT-DEFAULT 1-line-under-monolithic-threshold treatment per chat-112 PRECEDENT-EXTENSION threshold-not-near-threshold rule (299-line Block 1 at 1 line below 300 threshold treated identically to clear SMALL-MEDIUM Block 2; NO-RESUME of monolithic SKIP streak); (e) ai-bucket lint_priority preservation discipline (keep medium, do not flip to skip; keep lint_findings with large_python_block entry intact since Block 1 stays 299 above 80-95 post-distillation target threshold) NOW fourth consecutive chat-116-117-118-120 confirmation as ai-bucket-specific stable convention diverging from graphics-networking-physics-sprites-polish flip-to-skip discipline. chat-104 axis-bump rule confirmed quadruple inverse case: Block 1 was the largest tracked block AND used in-place-typing-import-bump (no line growth from existing-line edit) so example_solution_size_lines stays 299 AND all_blocks_largest_size stays 299; Block 2 grew 130 → 131 from typing import +1 only but NOT largest so practice_exercise_solution_size axis bumped 130 → 131 since Block 2 IS the practice-exercise-tracked block. chat-115 SCOPE-CLARIFICATION confirmed: chat-120 instance-attribute multi-target unpacks were ALL semicolon-multi-statement independent assignments (not tuple unpacks) so chat-115 tuple-unpack-must-split discipline did NOT trigger; inline annotation on existing semicolon-multi-statement bodies preserved compact shapes. chat-113 forward-ref-string-quoted confirmed: `'NPC'` quoted form required for all classes taking npc param in both Block 1 (State/IdleState/PatrolState/AlertState/ChaseState/StateMachine) and Block 2 (same set plus FleeState) since NPC defined later in same block; chat-120 reverses chat-118 Blackboard backward-ref-unquoted direction (both directions now confirmed working). chat-117 PEP 585 builtin tuple/list/dict confirmed: applied throughout Block 1 (NPC.position list[float], NPC.velocity list[float], NPC.player_pos tuple[float, float], NPC.home tuple[float, float], StateMachine.states dict[NPCState, State], StateMachine.history list[tuple[str, str, float]]) and Block 2 (NPC.pos list[float], NPC.vel tuple[float, float], NPC.player tuple[float, float]). chat-116 LOCAL-SCOPE locals-optional clause confirmed: PatrolState targets locals, AlertState wander locals, ChaseState dx/dy/d locals (Block 1) plus PatrolState.enter waypoints local, PatrolState.execute tx/ty/dx/dy/d locals, ChaseState and FleeState dx/dy/d locals (Block 2) all left unannotated. chat-112 PRECEDENT carry: pre-flight whitespace inspection confirmed Block 1 standard whitespace pattern (top-level class separators empty, method separators 4-space trailing, intra-method-body blanks 8-space or 12-space matching indent, single 12-space line at L200 preserved) and Block 2 dense compact pattern; all 17 anchors validated unique-occurrence pre-stage. **Compaction event mid-chat-120 absorbed via chat-117 Option-B-recovery PRECEDENT**: Block 1 commit landed pre-compaction (8 pairs clean first-try, file 82,673 → 83,156 bytes); transcript-driven resume validated all 9 Block 2 pairs locally against `ai_state_machines.html.block1_applied` (1 unique occurrence each); Block 2 dry-run-then-commit landed clean first-try post-resume; TWO-PHASE catalog flip Phase 1 + Phase 2 both committed clean first-try post-resume. TWO-PHASE catalog flip pattern reused per chat-110+...+118 PRECEDENT: Phase 1 surgical (4 pairs ~33 bytes consolidated entry status+coverage + practice_exercise_solution_size 130 → 131 + decrement pending 31 → 30 + insert linted_chat_120 + by_type_hint_coverage migration partial 16 → 15 plus full 20 → 21; **NO lint_priority flip** and **NO lint_findings clear** per ai-bucket lint_priority preservation discipline); Phase 2 notes-embed single pair (~8,472 bytes em-dash-free; this matches the `support/chat-120-notes.txt` artifact byte-for-byte at 8,433 chars); both phases committed clean first-try; catalog 342,109 → 350,614 bytes (delta +8,505 = Phase 1 +33 + Phase 2 +8,472). File size 82,673 → 83,794 bytes (+1,121 net from 1 line addition in Block 2 plus 17 inline annotation insertions across both blocks: 8 in Block 1, 9 in Block 2). AI module 4/5 → **5/5 complete = EIGHTH complete Phase-9 module** joining architecture (chat-92) + polish (chat-100) + networking (chat-105) + game_mathematics (chat-106) + physics (chat-109) + sprites (chat-111) + graphics (chat-115). High-priority counter: stays at 21/24+ (chat-120 closed medium-priority entry not high); cumulative 29 → 30 lessons linted. Stats final: by_priority high 4 medium 31 low 0 skip 29 UNCHANGED per ai-bucket precedent; by_lint_pass_status pending 30 + 30 linted_chat keys (added linted_chat_120: 1) + skipped 4; by_type_hint_coverage none 28 partial 15 full 21. Catalog file 342 KB → 350 KB (+8 KB from chat-120 notes embed). M0 disk-vs-catalog verification caught no drift per chat-119 carryforward (MINIMAL M0 per chat-119 cross-chat absorption): chat-118 flip confirmed remained landed on disk; chat-119 M0+scope-only closure pattern verified clean; broader sanity check OPTIONAL since chat-119 already did it cleanly at chat-119 M0. M2 closeout for chat-120 completed within turn (full bookkeeping landed after compaction-recovery mid-chat): wrote `support/archive/chat-120.md`, overwrote `support/next-chat-prompt.md` with chat-121 brief, overwrote `support/session.md` with chat-121 pointer, persisted `support/chat-120-notes.txt` at 8,433 bytes em-dash-free (continues chat-115/116/117/118 housekeeping discipline; chat-119 M0+scope-only had no notes payload), collapsed chat-120 IN PROGRESS section in this todo.md to closed stub, added chat-121 IN PROGRESS placeholder. OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-120.md`.

---

## Chat 119 — ✅ closed at M2 — Phase-9 lint cohort 29 STARTUP-ONLY (M0+scope-only deferral; no lint pass executed; 17-pair plan frozen for chat-120 execution)

Closed at M0+scope-only per Ray's explicit direction for budget-conscious cutover. Completed M0 disk-vs-catalog verification clean (Block 1 = 299 lines `language-python` labeled matches catalog axis 299; Block 2 = 130 lines bare `<pre><code>` matches catalog axis 130; python_block_count = 2 matches catalog; broadened regex per chat-114 + chat-116 caught both blocks correctly), broader sanity check on 3 prior-chat linted entries (ai_behavior_trees chat-118 + ai_flocking chat-117 + ai_decision_making chat-116 all clean), chat-118 carryforward verified landed on disk (`linted_chat_118: 1` present + ai_behavior_trees has linted_chat_118 status + chat_118_lint_pass_notes 6,701 chars + practice_exercise_solution_size 129 + stats by_priority 4/31/29 + by_type_hint_coverage 28/16/20 all match chat-118 close expectations), Decision-3 NO-SKIP confirmed (Block 1 = 299 < 300 monolithic threshold; monolithic streak stays PAUSED at 15 since chat-111), pre-flight whitespace inspection per chat-112 PRECEDENT (Block 1 has 45 trailing-ws blank lines mixed 4-space + 8-space + 8 pure-empty lines; Block 2 has 12 pure-empty lines and ZERO trailing-ws blanks). Full 17-pair atomic-edit plan composed: Block 1 (8 pairs, ZERO line growth) = typing-import-in-place-bump per chat-116 PRECEDENT + 7 per-class full-body rewrites per chat-118 PRECEDENT; Block 2 (9 pairs, +1 line growth) = from-scratch typing-import per chat-104 PRECEDENT + 8 per-class full-class rewrites with chat-115 semicolon-multi inline annotations. TWO-PHASE catalog flip projection per chat-110+ PRECEDENT: Phase 1 surgical ~1.5 KB (5 pairs) + Phase 2 notes-embed ~6-7 KB em-dash-free. NEW chat-119 CLOSURE-PATTERN-PRECEDENT: M0+scope-only closure for budget-conscious cutover. M2 closeout for chat-119 written in-chat: overwrote `support/next-chat-prompt.md` with chat-120 brief baking in the staged 17-pair plan, overwrote `support/session.md` with chat-120 pointer, this `todo.md` update collapses chat-119 IN PROGRESS to this closed stub + adds chat-120 IN PROGRESS placeholder above, wrote `support/archive/chat-119.md` with full M0 audit + scope analysis preserved. NO `support/chat-119-notes.txt` persisted (no lint pass payload to record; differs from chat-117/118 housekeeping pattern in this respect; chat-120 M0 should NOT flag this as missing). NO catalog flip landed (ai_state_machines still `pending` with stats unchanged; catalog 342,109 bytes from chat-118 close). NO file edits to ai_state_machines.html (9,307 bytes unchanged). OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-119.md`.

---

## Chat 118 — ✅ closed at M2 via chat-119 M0 absorption (Phase-9 lint cohort 28; SOFT-DEFAULT ai_behavior_trees.html confirmed clean at M0)

`ai_behavior_trees.html` linted (Decision 3 NO-SKIP at 264 max-block well below 300-line monolithic threshold; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-SEVENTH consecutive matching chat-104/106/107/108/110/111/112/113/114/115/116/117 shape with 13 classes Block 1 behavior tree node hierarchy (NodeState + BehaviorNode + Selector + Sequence + Parallel + RandomSelector + Decorator + Inverter + Repeater + Cooldown + Condition + Action + Blackboard + BehaviorTree) and 7 classes plus 9 free functions plus pygame main loop Block 2 worker AI simulation; Decision 2 MEDIUM sweep at 28 atomic edit pairs across Blocks 1-2 via two Filesystem:edit_file calls dry-run-then-commit clean first-try with zero anchor failures (MEDIUM-bucket symmetric 14+14 split per chat-105 PRECEDENT extending chat-117 SMALL-to-MEDIUM 6+12 asymmetric pattern to symmetric MEDIUM scale; chat-118 NEW PRECEDENT 14+14 symmetric split balances per-call payload sizes when both blocks have comparable lint surface); Block 1 14 pairs (typing-import-in-place-bump per chat-116 PRECEDENT existing `from typing import List, Optional, Callable` bumped alphabetized to `Any, Callable, List, Optional` no line growth + 13 per-class atomic full-body rewrites with class-declaration-line natural disambiguation); Block 2 14 pairs (from-scratch typing-import per chat-104 PRECEDENT new `from typing import Any, Callable, Optional` line +1 + Node.__init__ tuple-unpack-must-split per chat-115 PRECEDENT +2 lines + Cooldown(Node).__init__ tuple-unpack-must-split per chat-115 PRECEDENT +3 lines + Composite semicolon-multi-statement inline annot per chat-115 + Composite.add string-quoted forward-ref per chat-113 PRECEDENT + Selector/Sequence full-class-rewrite tick-sig-only per chat-118 PRECEDENT + Cond/Act semicolon-multi-statement inline annot per chat-115 + combined free-function sweep covering move_to_res/move_home/pickup/deposit in 1 atomic pair per chat-118 NEW MICRO-PRECEDENT-CONFIRMATION via contiguous-block anchor 8 sequential lines wrapping 4 functions + build_tree PEP 585 builtin tuple per chat-117 PRECEDENT + draw_tree/dist annotations + flee/move_to with locals-optional preservation of internal tuple unpacks per chat-116 LOCAL-SCOPE clause); ~50+ total annotations applied). NEW chat-118 MICRO-PRECEDENT-CONFIRMATIONS: (a) per-class atomic rewrite as default for many-classes-in-one-block lessons (13 classes × 1 atomic pair Block 1 versus per-method splits which would have been 30+ pairs; class declaration line provides natural disambiguation; full-body anchors avoid method-signature-collision risk between Selector-Sequence shared-shape tick methods); (b) combined free-function sweep via contiguous-block anchor (N sequential free functions combined into 1 atomic pair); (c) MEDIUM-bucket symmetric 14+14 split extending chat-117 SMALL-to-MEDIUM 6+12 asymmetric pattern to symmetric MEDIUM scale; (d) existing-typing-import-in-place-bump per chat-116 AND from-scratch per chat-104 coexisting within one lesson when Blocks have differing pre-existing typing surface. chat-104 axis-bump rule confirmed triple inverse case: Block 1 was the largest tracked block AND used in-place-typing-import-bump (no line growth from existing-line edit) so example_solution_size_lines stays 264 AND all_blocks_largest_size stays 264; Block 2 grew 123 → 129 from typing import +1 plus 2 tuple-unpack splits +5 but NOT largest so practice_exercise_solution_size axis bumped 123 → 129 (Block 2 IS the practice-exercise-tracked block). chat-115 tuple-unpack-must-split confirmed: applies cleanly within single lesson at multiple sites (Node.__init__ self.name self.last 2-tuple split +2 lines; Cooldown-Node.__init__ self.child self.secs self.t0 3-tuple split +3 lines; total +5 lines from tuple-unpack alone across both classes). chat-113 string-quoted forward-ref confirmed: applies cleanly to Composite class in ai_behavior_trees Block 2 (`def add(self, c: Node) -> 'Composite':` string-quoted self-referential return since Composite being defined; Node backward-ref unquoted since defined earlier). chat-117 PEP 585 builtin tuple confirmed: applies cleanly to build_tree return type (`def build_tree() -> tuple[Selector, Sequence]:` no typing import needed for tuple itself). chat-112 PRECEDENT carry: pre-flight whitespace inspection on Block 1 blank-line whitespace pattern (top-level-class-separators empty string, method-separators 4-space trailing, intra-method-body 8-space or 12-space matching indent); per-class atomic rewrites faithfully reproduced all blank-line padding; all 14 Block 1 anchors validated unique-occurrence pre-stage. chat-114 + chat-116 M0 catalog-vs-regex python_block_count reconciliation confirmed: Block 1 used class-on-pre form (language-python labeled); Block 2 used bare-pre-code form; broadened regex correctly captured both. TWO-PHASE catalog flip pattern reused per chat-110+...+117 PRECEDENT: Phase 1 surgical (5 pairs ~1.5 KB consolidated entry status+coverage + practice_exercise_solution_size 123 → 129 + decrement pending 32 → 31 + insert linted_chat_118 + by_type_hint_coverage migration partial 17 → 16 plus full 19 → 20); Phase 2 notes-embed single pair (~6.7 KB em-dash-free); both phases committed clean first-try; catalog 335,335 → 342,109 bytes (delta +6,774). File size 89,236 → 90,053 bytes. AI module 3/5 → **4/5 partial**. High-priority counter: stays at 21/24+ (chat-118 closed medium-priority entry not high); cumulative 28 → 29 lessons linted. Stats final: by_priority high 4 medium 31 skip 29; by_lint_pass_status pending 31 + 29 linted_chat keys (added linted_chat_118: 1) + skipped 4; by_type_hint_coverage none 28 partial 16 full 20. Catalog file 335 KB → 342 KB (+7 KB from chat-118 notes embed). M0 disk-vs-catalog verification caught no drift: chat-117 flip confirmed landed on disk (linted_chat_117: 1 present + chat_117_lint_pass_notes 4865 bytes + ai_flocking axes match); broader sanity check on ai_flocking chat-117 + ai_decision_making chat-116 + graphics_ui_hud chat-115 all clean. M2 closeout for chat-118 partially completed at chat-118 close (only `support/next-chat-prompt.md` was overwritten with chat-119 brief; catalog flip landed clean); remaining four M2 artifacts landed at chat-119 M0 cross-chat absorption: `support/chat-118-notes.txt` persisted at 6702 bytes em-dash-free (continues chat-117 housekeeping discipline), wrote `support/archive/chat-118.md`, overwrote `support/session.md` with chat-119 pointer, collapsed chat-118 IN PROGRESS section in this todo.md to closed stub, added chat-119 IN PROGRESS placeholder. OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-118.md`.

---

## Chat 117 — ✅ closed at M2 (Phase-9 lint cohort 27; SOFT-DEFAULT ai_flocking.html confirmed clean at M0)

`ai_flocking.html` linted (Decision 3 NO-SKIP at 249 max-block well below 300-line monolithic threshold; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-SIXTH consecutive with TWO classes per block (Block 1: Vector2D + Boid; Block 2: V + Boid) behavior-rich shape; Decision 2 SMALL-to-MEDIUM sweep at 18 atomic edit pairs across Blocks 1-2 via two Filesystem:edit_file calls dry-run-then-commit clean first-try with zero anchor failures (6 pairs Block 1 first call + 12 pairs Block 2 second call per chat-105 per-block-group SMALL-to-MEDIUM-bucket asymmetric-split precedent; chat-117 NEW PRECEDENT 6+12 asymmetric split shape viable for blocks with imbalanced pre-existing annotation coverage extending chat-116 MEDIUM 3+20 pattern to smaller scale); Block 1 surgical 6 pairs (Vector2D.__init__ full-body-rewrite per chat-107 PRECEDENT with self.x: float + self.y: float + -> None; Boid.__init__ full-body-rewrite annotating all 14 instance attrs with Vector2D / float / int / tuple[int, int, int] PEP 585 builtin + -> None; Boid.flock/update/edges/draw signature-only -> None); Block 2 from-scratch 12 pairs (V class one-line-body methods retained inline-body shape with string-quoted forward-refs (s, o: 'V') -> 'V' on add/sub/mul/div plus -> 'V' on norm/limit plus -> float on mag plus reynolds free-function (boids: list['Boid'], idx: int, w: dict[str, float]) -> 'V'; V.__init__ tuple-unpack-must-split per chat-115 NEW MICRO-PRECEDENT s.x, s.y = x, y split into 3 lines +2 line growth; Boid.__init__ similarly split s.p, s.a = V(x, y), V() into per-target form +1 line growth plus added s.last: tuple[V, V, V] = (V(), V(), V()) PEP 585 builtin tuple annotation; Boid.can_see + Boid.step signature-only annotations); ~31 total annotations applied). NEW chat-117 MICRO-PRECEDENT-CONFIRMATION primary: string-quoted forward-refs for self-referential types in same block scale cleanly to compact-shorthand-class case ('V' works identically to 'Vector2D' style; the quoted form is the type name regardless of class identifier length; useful in lessons that use compact pedagogical shorthand class names). NEW chat-117 MICRO-PRECEDENT-CONFIRMATION secondary: PEP 585 builtin tuple[X, Y, Z] annotation for fixed-arity heterogeneous-type instance attribute requires no typing import when all element types are in scope at annotation time per PEP 585 Python 3.9+ semantics; avoids from typing import Tuple. NEW chat-117 PRECEDENT-CONFIRMATION: SMALL-to-MEDIUM-bucket 6+12 asymmetric split-call viable for blocks with imbalanced pre-existing annotation coverage (Block 1 heavily-typed needed only 6 surgical pairs; Block 2 fully-unannotated needed 12 from-scratch pairs); extends chat-116 MEDIUM-bucket 3+20 asymmetric pattern at smaller scale. chat-117 INVERSE CASE on chat-116 in-place-typing-import-bump: when block has existing typing import but lint pass needs NO new typing names (all forward-refs string-quoted, all builtins available via PEP 585), the existing typing import is left untouched (no in-place bump needed; ZERO line-count change; Block 1 example_solution_size_lines stays 249). chat-115 tuple-unpack-must-split confirmed: applies cleanly to compact-shorthand-class case (V.__init__ s.x, s.y = x, y split +2 lines; Boid.__init__ s.p, s.a = V(x, y), V() split +1 line for total +3 line growth in Block 2 99 → 102). chat-104 axis-bump rule confirmed inverse case: Block 1 had existing typing import left untouched so example_solution_size_lines stays 249 even though Block 1 IS the largest tracked block; Block 2 grew 99 → 102 from tuple-unpack splits but NOT largest so example_solution_size_lines unchanged BUT practice_exercise_solution_size axis bumped 99 → 102 since Block 2 IS the practice-exercise-tracked block. chat-107 PRECEDENT carry: full-body-rewrite for __init__ provides natural class-specific disambiguation (Vector2D.__init__ vs Boid.__init__ multi-line bodies differ entirely). chat-112 PRECEDENT carry: pre-flight whitespace inspection on Block 1 found 53 blank lines with 4/8/12-space trailing whitespace heavy (Block 2 zero blank-line trailing whitespace clean); Boid.__init__ multi-line anchor faithfully reproduced 4 blank-with-8-space blanks; committed clean first-try with no late-discovery anchor failures. chat-114 + chat-116 PRECEDENT M0 catalog-vs-regex python_block_count reconciliation confirmed CRITICAL again: labeled-on-code regex returned ZERO hits; broadened regex per chat-116 zero-labeled extension correctly identified both class-on-pre form blocks. TWO-PHASE catalog flip pattern reused per chat-110+...+116 PRECEDENT: Phase A 4 surgical edits (consolidated entry status+coverage+practice_size + insert linted_chat_117 + decrement pending + by_type_hint_coverage migration ~0.7 KB) plus Phase B notes-embed single pair (~4.9 KB em-dash-free); both phases committed clean first-try; catalog 330,397 → 335,335 bytes (delta +4,938). AI module 2/5 → **3/5 partial**. High-priority counter: stays at 21/24+ (chat-117 closed medium-priority entry not high); cumulative 27 → 28 lessons linted. Stats final: by_priority high 4 medium 31 skip 29; by_lint_pass_status pending 32 + 28 linted_chat keys (added linted_chat_117: 1) + skipped 4; by_type_hint_coverage none 28 partial 17 full 19. Catalog file 330 KB → 335 KB (+5 KB from chat-117 notes embed). M0 disk-vs-catalog verification caught no drift: chat-116 flip confirmed landed on disk (linted_chat_116: 1 present + chat_116_lint_pass_notes 5794 chars + ai_decision_making axes match); broader sanity check on ai_decision_making chat-116 + graphics_ui_hud chat-115 + graphics_lighting chat-114 all clean. M2 closeout for chat-117 completed within turn (after compaction-recovery mid-chat-117 and cap-deferral on first M2 attempt; Option-B-recovery via Ray-directed go-ahead landed all artifacts): wrote `support/archive/chat-117.md`, overwrote `support/next-chat-prompt.md` with chat-118 brief, overwrote `support/session.md` with chat-118 pointer, persisted `support/chat-117-notes.txt` at 4865 bytes em-dash-free (continues chat-116 housekeeping discipline), collapsed chat-117 IN PROGRESS section in todo.md to closed stub, added chat-118 IN PROGRESS placeholder. OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-117.md`.

---

## Chat 116 — ✅ closed at M2 (Phase-9 lint cohort 26; SOFT-DEFAULT ai_decision_making.html confirmed clean at M0)

`ai_decision_making.html` linted (Decision 3 NO-SKIP at 153 max-block well below 300-line monolithic threshold; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-FIFTH consecutive with UtilityAI behavior-rich ONE-class 10+ methods shape; Decision 2 MEDIUM sweep at 23 atomic edit pairs across Blocks 1-2 via two Filesystem:edit_file calls dry-run-then-commit clean first-try with zero anchor failures (3 pairs Block 1 first call + 20 pairs Block 2 second call per chat-105 per-block-group MEDIUM-bucket split precedent; chat-116 NEW PRECEDENT 3+20 asymmetric split shape viable for blocks with heavily imbalanced pre-existing annotation coverage where Block 1 needed only 3 pairs due to extensive pre-existing annotations while Block 2 needed 20 pairs as essentially-blank starting state); Block 1 UtilityAI 3 pairs (typing import in-place bump adding Any + __init__ full-body-rewrite + setup_actions return); Block 2 19 top-level module functions 20 pairs (1 typing import from scratch + 19 def signatures covering response curves linear/quadratic/exponential/sigmoid/gaussian + helpers dist_to/nearest + predicates can_eat/fight/flee/explore + scorers score_eat/fight/flee/explore + personality_mod + decide + take_action + tick_needs); ~38 total annotations applied). NEW chat-116 MICRO-PRECEDENT: in-place typing-import bump for blocks with existing typing line yields ZERO line-count growth (Block 1 `from typing import Dict, List, Tuple, Optional` bumped in-place to `from typing import Any, Dict, List, Optional, Tuple` alphabetized; contrast with chat-104 from-scratch +1 line growth case where typing import added as new line). CALIBRATION: use in-place bump when existing typing import line is present in the block; use from-scratch when block has ZERO typing imports. NEW chat-116 MICRO-PRECEDENT secondary: zero-labeled-blocks regex-broadening case extends chat-114 catalog-vs-regex python_block_count reconciliation discipline to ZERO-labeled case (this lesson used class-on-pre form for Block 1 and bare-pre-code form for Block 2; labeled-on-code regex `<pre class="language-python"><code>` returned ZERO hits; broadened regex `<pre[^>]*><code[^>]*>` correctly identified both blocks). chat-116 SCOPE-CLARIFICATION on chat-115 tuple-unpack-must-split: applies to INSTANCE-ATTRIBUTE multi-target unpacks `self.x, self.y = ...`; LOCAL-SCOPE tuple unpacks within a def `dx, dy = f[0]-a.pos[0], f[1]-a.pos[1]` fall under chat-106 locals-optional clause and require no annotation. chat-104 axis-bump rule confirmed in-place-bump case: Block 1 typing-import bumped in-place so example_solution_size_lines stays 153 (no line-count growth from in-place edit even though Block 1 IS largest tracked block); Block 2 grew 143 → 144 from new-line typing import addition but NOT largest so example_solution_size_lines unchanged BUT practice_exercise_solution_size axis bumped 143 → 144 since Block 2 IS the practice-exercise-tracked block. chat-107 PRECEDENT carry: full-body-rewrite for __init__ provides natural class-specific disambiguation. chat-112 PRECEDENT carry: pre-flight whitespace inspection on blank lines (Block 1 32 trailing-whitespace blank lines with 4/8/12-space variants; chat-116 plan avoided multi-line oldText anchors spanning blank lines via single-line and 2-line anchors only; Block 2 zero blank-line trailing whitespace as clean module-level code). NO chat-115 NEW MICRO-PRECEDENT triggers (no INSTANCE-ATTRIBUTE multi-target tuple unpacking; no Liskov class hierarchy since Block 1 has no inheritance and Block 2 has no classes). TWO-PHASE catalog flip pattern reused per chat-110+111+112+113+114+115 PRECEDENT: Phase A 4 surgical edits (consolidated entry status+coverage + 2 stats decrements/inserts + 1 by_type_hint_coverage migration ~0.7 KB) + Phase B notes-embed single pair (~5.8 KB em-dash-free); both phases dry-run-then-commit clean first-try; catalog 324,530 → 330,397 bytes (delta +5,867). AI module 1/5 → **2/5 partial**. High-priority counter: stays at 21/24+ (chat-116 closed medium-priority entry not high); cumulative 26 → 27 lessons linted. Stats final: by_priority high 4 medium 31 skip 29; by_lint_pass_status pending 33 + 27 linted_chat keys (added linted_chat_116: 1) + skipped 4; by_type_hint_coverage none 28 partial 18 full 18. Catalog file 324 KB → 330 KB (+6 KB from chat-116 notes embed). M0 disk-vs-catalog verification caught no drift: chat-115 flip confirmed landed on disk (linted_chat_115: 1 present + chat_115_lint_pass_notes payload at 13.6 KB; brief estimated 8 KB; within 2-22 KB band so no repair); broader sanity check on graphics_ui_hud chat-115 + graphics_lighting chat-114 + graphics_postprocessing chat-113 all clean. chat-114 M0 catalog-vs-regex discipline confirmed CRITICAL for this lesson (initial labeled-on-code regex returned ZERO blocks; broadened regex per chat-114 PRECEDENT + chat-116 extension correctly identified both blocks). M2 closeout for chat-116 completed within turn (compaction event mid-chat-116 but M2 still landed): wrote `support/archive/chat-116.md`, overwrote `support/next-chat-prompt.md` with chat-117 brief, overwrote `support/session.md` with chat-117 pointer, persisted `support/chat-116-notes.txt` at 5795 bytes em-dash-free (addresses chat-112-notes.txt gap precedent), collapsed chat-116 IN PROGRESS section in todo.md to closed stub, added chat-117 IN PROGRESS placeholder. OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-116.md`.

---

## Chat 115 — ✅ closed at M2 (Phase-9 lint cohort 25; SOFT-DEFAULT graphics_ui_hud.html confirmed clean at M0)

`graphics_ui_hud.html` linted (Decision 3 NO-SKIP at 293 max-block 7 lines below 300-line monolithic threshold; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-FOURTH consecutive matching chat-104/106/107/108/110/111/112/113/114 shape with 11 unique behavior-heavy UI classes (UIElement abstract base + HealthBar/Minimap/DialogBox/UIManager Block 1 + Inventory/NotificationSystem/TooltipSystem Block 2 + UIElement/HealthBar/Minimap/DialogBox/FloatNumber/UIManager Block 3) all retaining manual __init__; Decision 2 LARGE sweep at 37 atomic edit pairs across Blocks 1-3 via two Filesystem:edit_file calls = **LARGEST PHASE-9 SWEEP TO DATE by pair count** eclipsing chat-111 sprite_groups_layers 33 pairs; LARGE-bucket split per chat-110+111 PRECEDENT into 18 pairs Blocks 1+2 first call + 19 pairs Block 3 second call; Blocks 1+2 commit second-try after first-try typo-recovery in TooltipSystem.set_tooltip oldText (atomic rollback held; file unchanged at 60,636 bytes); Block 3 commit clean first-try; Block 1 UIElement+HealthBar+Minimap+DialogBox+UIManager 14 pairs (no typing import; existing `from typing import Dict, List, Optional, Tuple` sufficed); Block 2 Inventory+NotificationSystem+TooltipSystem 4 pairs (1 typing import + 3 combined init+method pairs); Block 3 UIElement+HealthBar+Minimap+DialogBox+FloatNumber+UIManager+game-loop 19 pairs (no typing import); total annotations: ~113 individual annotations across 37 atomic pairs (1 typing import line Block 2 only + ~38 return arrows + ~46 new parameter annotations + ~28 instance-attribute annotations)). NEW chat-115 MICRO-PRECEDENT: tuple-unpack-must-split discipline for multi-target tuple unpacking. When a single line contains multi-target tuple unpacking like `self.x, self.y, self.z = x, y, z`, Python annotation syntax forbids per-target annotations on the unpack line; the pragmatic solution is to split into N individual annotated assignments adding (N - 1) lines. L82 4-tuple-unpack in FloatNumber.__init__ split adding +3 lines bringing Block 3 from 165 → 168. CALIBRATION: if RHS is non-trivial like `self.x, self.y = compute(seed)`, split would require temporary or duplicating call; consider leaving unannotated as locals-optional fallback per chat-106 PRECEDENT or hoisting the call. NEW chat-115 MICRO-PRECEDENT secondary: pragmatic-Liskov-widening on abstract-base pass-body return annotations as alternative to chat-113 strict-Liskov-safe pattern when typing-import minimization preferred (UIElement.update → None base with FloatNumber.update → bool override widening accepted; trades typing-strictness for typing-import minimization). CALIBRATION: use pragmatic widening when (a) block has zero pre-existing typing imports, (b) Optional would be SOLE reason to add typing import, and (c) override-site widening is at most 1-2 sites. NEW chat-115 PRECEDENT: 37-pair LARGE-bucket split-call at 18+19 split shape viable (extends chat-110 17+16 and chat-111 20+13 demonstrations to higher pair count and even-split shape). chat-104 axis-bump rule confirmed triple inverse case: Block 1 received no typing import (existing one sufficed) so example_solution_size_lines stays 293 even though Block 1 IS the largest tracked block (chat-115 inverse to chat-114 receiving-block-was-largest case); Block 2 grew 63 → 65 from typing import + blank line but NOT largest so no axis bump (chat-112 inverse case match); Block 3 grew 165 → 168 from tuple-unpack-split but NOT largest tracked block so example_solution_size_lines unchanged BUT practice_exercise_solution_size axis bumped 165 → 168 since Block 3 IS the practice-exercise-tracked block. chat-113+chat-112+chat-110+chat-111+chat-114 PRECEDENTS carry: backward-ref unquoted annotations (UIManager.__init__ dict[str, UIElement] + list[FloatNumber] both backward refs); pre-flight whitespace inspection on blank lines (Block 1 47 trailing-whitespace blank lines = 12×4-space + 34×8-space + 1×12-space; Block 2 5 blank lines 8-space at L11+L15+L32+L44+L56; Block 3 ZERO blank-line trailing whitespace); TWO-PHASE catalog flip Phase A surgical (5 pairs ~1.5 KB consolidated entry replace + 3 stats edits + 1 linted_chat_115 insertion) + Phase B notes-embed single pair (~8 KB em-dash-free); both phases dry-run-then-commit clean first-try; catalog 310,628 → 324,530 bytes (delta +13,902); honest-annotation-matches-runtime-type for mutated instance attrs (FloatNumber.self.y annotated as float since `self.y -= 40 * dt` promotes to float after one tick). Body-anchor strategy disambiguates 4 collision-prone def update(self, dt): + 4 collision-prone def render(self, surf): across HealthBar/Minimap/DialogBox/FloatNumber/UIManager via unique first-body-line content (HealthBar.update has # Exponential approach comment; DialogBox.update has if not self.active: return + if self.progress &lt; len(self.text); FloatNumber.update has self.life += dt; UIManager.update has for e in self.elements.values(): e.update(dt); render variants disambiguate via unique pygame.draw.rect color tuples). Recovery event: first commit attempt of Block 1+2 (18 pairs) failed with zero-match on TooltipSystem.set_tooltip pair due to typo in retyped oldText (an accidental `-> None` had crept into the oldText copy); atomic rollback confirmed file unchanged at 60,636 bytes (Filesystem:edit_file MCP rollback semantics held cleanly per chat-110 PRECEDENT); re-staged from verified JSON file at `/tmp/edit_pairs/b1b2_pairs.json` and re-commit landed clean second-try. Graphics module 4/5 → **5/5 complete = SEVENTH complete Phase-9 module** joining architecture (chat-92) + polish (chat-100) + networking (chat-105) + game_mathematics (chat-106) + physics (chat-109) + sprites (chat-111). High-priority counter: stays at 21/24+ (chat-115 closed medium-priority entry not high); cumulative 25 → 26 lessons linted. Stats final: by_priority high 4 medium 31 skip 29; by_lint_pass_status pending 34 + 26 linted_chat keys (added linted_chat_115: 1) + skipped 4; by_type_hint_coverage none 28 partial 19 full 17. Catalog file 310 KB → 324 KB (+14 KB from chat-115 notes embed). M0 disk-vs-catalog verification caught no drift: chat-114 flip confirmed landed on disk (linted_chat_114: 1 present + chat_114_lint_pass_notes payload + graphics_lighting entry axes match); broader sanity check on graphics_lighting chat-114 + graphics_postprocessing chat-113 + graphics_shaders chat-112 all clean (typing imports, return arrows, instance-attribute annotations, string-quoted forward-refs all present). M2 closeout for chat-115 completed within turn (no tool-use cap hit): wrote `support/archive/chat-115.md`, overwrote `support/next-chat-prompt.md` with chat-116 brief, overwrote `support/session.md` with chat-116 pointer, persisted `support/chat-115-notes.txt` at ~8 KB clean, collapsed chat-115 IN PROGRESS section in todo.md to closed stub, added chat-116 IN PROGRESS placeholder. OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-115.md`.

---

## Chat 114 — ✅ closed at M2 (Phase-9 lint cohort 24; SOFT-DEFAULT graphics_lighting.html confirmed clean at M0)

`graphics_lighting.html` linted (Decision 3 NO-SKIP at 216 max-block well below 300-line monolithic threshold; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-THIRD consecutive matching chat-104/106/107/108/110/111/112/113 shape with 6 classes LightingSystem+Light+DirectionalLight+PointLight+SpotLight+ShadowMapper all retaining manual __init__ due to non-trivial side effects (init_lights call, super-chain reassignments, framebuffer creation); Decision 2 SMALL sweep at 13 atomic edit pairs across Blocks 1-3 via two Filesystem:edit_file calls dry-run-then-commit clean first-try with zero anchor failures and no recovery needed; first call 11 pairs Blocks 1+2; second call 2 pairs Block 3 after mid-pass Block-3-discovery from initial regex narrow match; Block 1 LightingSystem+Light+DirectionalLight+PointLight+SpotLight 7 pairs (1 typing import + 5 __init__ handling + 1 create_lighting_shader return); Block 2 ShadowMapper 4 pairs (1 typing import + 1 __init__ + 2 method signatures); Block 3 Practice Exercise example-solution 2 pairs (2 top-level function signatures, no classes); total annotations: 2 typing import lines, 11 return arrows, 7 new parameter annotations, 19 instance-attribute annotations = 39 individual annotations). NEW chat-114 PRECEDENT CANDIDATE: M0 catalog-vs-regex python_block_count reconciliation. Initial regex pattern `<pre class="language-python"><code>` missed Block 3 which uses bare `<pre><code>` inside `<details><summary>✅ Example Solution</summary>...</details>` wrapper. Catalog's `python_block_count=3` vs initial regex's `2` mismatch surfaced during pre-lint inventory review and triggered broader-regex re-scan. Future chats: when catalog `python_block_count` exceeds initial regex hit count, broaden regex to `<pre[^>]*><code[^>]*>(.*?)</code></pre>` to also match bare `<pre><code>` blocks (practice-exercise example-solutions in this codebase commonly use bare form rather than labeled language-python form). NEW chat-114 PRECEDENT: Liskov-respecting subclass __init__ signature-only when subclass reassigns inherited attrs only (DirectionalLight signature-only `-> None`; reassignments inherit base-class annotation type per Liskov convention); full-body-rewrite required for subclasses adding new attrs (PointLight + SpotLight). chat-104 axis-bump rule confirmed dual case: Block 1 grew 215 → 216 lines from `from typing import Any` line addition AND Block 1 IS the largest tracked block in the lesson so BOTH `example_solution_size_lines` and `all_blocks_largest_size` bumped 215 → 216; contrasts with Block 2 which grew 57 → 59 from typing import + blank line BUT Block 2 is NOT the largest tracked block so NO axis bump (chat-112 inverse case match). chat-113+chat-112+chat-110+chat-111 PRECEDENTS carry: string-quoted forward-refs for self-referential same-block types (`list["Light"]` in LightingSystem.__init__ since Light defined later in same Block 1); pre-flight whitespace inspection on blank lines (Block 1 8-space trailing on lines 12+15 of Block 1 region preserved verbatim; Block 2 4-space trailing on line 14 of Block 2 region preserved verbatim); TWO-PHASE catalog flip Phase A surgical (7 pairs ~1.5 KB consolidated entry replace + 6 stats edits) + Phase B notes-embed single pair (7,031 chars em-dash-free); both phases dry-run-then-commit clean first-try; catalog 303,631 → 310,628 bytes (delta +6,997 within projection band); round-trip byte-for-byte verified post-commit (parsed catalog notes match source payload exactly). Anchor disambiguation: pair 4 DirectionalLight.__init__ signature-only vs pairs 2/5/6/7 (LightingSystem/Light/PointLight/SpotLight __init__ full-body-rewrites) all share literal first line `def __init__(self, ...):` but multi-line bodies differ entirely (5 distinct full-body-rewrites with distinct attribute assignments providing natural class-specific disambiguation per chat-107+chat-110/111/112 precedent). Graphics module 3/5 → **4/5 partial**. High-priority 20/24+ → 21/24+; cumulative 24 → 25 lessons linted. Stats final: by_priority high 4 medium 32 skip 28; by_lint_pass_status pending 35 + 25 linted_chat keys (added linted_chat_114: 1) + skipped 4; by_type_hint_coverage none 28 partial 20 full 16. Catalog file 303 KB → 310 KB (+7 KB from chat-114 notes embed). M0 disk-vs-catalog verification caught no drift: chat-113 flip confirmed landed on disk (linted_chat_113: 1 present + chat_113_lint_pass_notes 7163 bytes); broader sanity check on graphics_postprocessing chat-113 + graphics_shaders chat-112 + sprite_groups_layers chat-111 all clean. M2 closeout for chat-114 completed within turn (no tool-use cap hit): wrote `support/archive/chat-114.md`, overwrote `support/next-chat-prompt.md` with chat-115 brief, overwrote `support/session.md` with chat-115 pointer, persisted `support/chat-114-notes.txt` at ~7.1 KB clean, collapsed chat-114 IN PROGRESS section in todo.md to closed stub, added chat-115 IN PROGRESS placeholder. OPEN FOLLOW-UPS carried forward: physics_bounce_friction catalog type_hint_coverage=none stale (chat-107 lint pass annotated 4 blocks fully but axis-refresh never landed); chat-112-notes.txt missing from disk (low-priority housekeeping gap; catalog-internal payload intact). See `support/archive/chat-114.md`.

---

## Chat 113 — ✅ closed at M2 via chat-114 M0 absorption (Phase-9 lint cohort 23; SOFT-DEFAULT graphics_postprocessing.html confirmed clean at M0)

`graphics_postprocessing.html` linted (Decision 3 NO-SKIP at 151 / 152 max-block well below 300-line monolithic threshold; monolithic-bucket SKIP streak remains PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-SECOND consecutive matching chat-104+106+107+108+110+111+112 shape with 3 classes PostProcessingPipeline+PostProcessEffect+BloomEffect all retaining manual __init__ due to non-trivial side effects (self.init_pipeline call, self.create_shader call, super().__init__ call respectively); Decision 2 SMALL sweep at 15 atomic edit pairs across Blocks 1-2 via single Filesystem:edit_file call dry-run-then-commit clean first-try with zero anchor failures and no recovery needed; Block 1 (10 pairs): 1 typing import added + 3 __init__ full-body-rewrites + 6 method signatures; Block 2 (5 pairs): 5 top-level function signatures, no classes; total annotations: 1 typing import line, 10 method/function return arrows, 8 new parameter annotations, 13 instance-attribute annotations across the 3 __init__ full-body-rewrites). NEW chat-113 PRECEDENT: string-quoted forward-references for self-referential types in same block (`dict[str, "PostProcessEffect"]` when PostProcessEffect defined later in same block) plus unquoted backward-references OK when referenced class appears earlier (`pipeline: PostProcessingPipeline` parameter). NEW chat-113 PRECEDENT: Liskov-safe covariant return narrowing for subclass overrides of base pass-body methods (`Optional[moderngl.Program]` for PostProcessEffect.create_shader base returning None implicitly via pass; tighter `moderngl.Program` for BloomEffect.create_shader subclass override). chat-104 axis-bump rule confirmed: Block 1 grew 151 → 152 lines from `from typing import Optional` line addition AND Block 1 IS the largest tracked block in the lesson so BOTH `example_solution_size_lines` and `all_blocks_largest_size` bumped 151 → 152; contrasts with chat-112 inverse case where Block 1 grew 101 → 102 but Block 1 was not the largest (Block 3 at 109 was). chat-112+chat-110+chat-111 PRECEDENTS carry: pre-flight whitespace inspection on blank lines (Block 1 8-space trailing on internal blank lines preserved in pair 2 PostProcessingPipeline.__init__ full-body-rewrite); TWO-PHASE catalog flip Phase A surgical (5 pairs ~1.5 KB) + Phase B notes-embed single pair (7,163 chars em-dash-free); both phases dry-run-then-commit clean first-try; catalog 296,426 → 303,631 bytes (delta +7,205 matching projection). Anchor disambiguation: pair 7 PostProcessEffect.create_shader vs pair 10 BloomEffect.create_shader both share literal `def create_shader(self):` first line but second-line bodies differ (base docstring vs subclass vertex shader triple-string); pairs 6 PostProcessEffect.__init__ vs pair 9 BloomEffect.__init__ both share literal `def __init__(self, pipeline):` first line but multi-line bodies are completely different (base sets self.pipeline/ctx/enabled/shader; subclass calls super().__init__ then sets self.threshold/intensity/radius). Graphics module 2/5 → **3/5 partial**. High-priority 19/24+ → 20/24+; cumulative 23 → 24 lessons linted. Stats final: by_priority high 4 medium 33 skip 27; by_lint_pass_status pending 36 + 24 linted_chat keys (added linted_chat_113: 1) + skipped 4; by_type_hint_coverage none 28 partial 21 full 15. Catalog file 290 KB → 303 KB (+13 KB from chat-113 notes embed). M0 disk-vs-catalog verification caught no drift: chat-112 flip confirmed landed on disk (linted_chat_112: 1 present + chat_112_lint_pass_notes 7,844 bytes); broader sanity check on graphics_shaders chat-112 + sprite_groups_layers chat-111 + sprite_animation chat-110 all clean. M0 SEPARATE FINDING: housekeeping artifact `support/chat-112-notes.txt` missing from disk despite chat-112 close brief claiming a 7.8 KB persist; catalog-internal payload intact (catalog flip was clean) so this is low-priority housekeeping-only artifact gap; future cleanup chat should re-attempt OR batch-clean. M2 closeout for chat-113 hit tool-use cap before bookkeeping artifacts landed; chat-114 M0 absorbed M2 closeout: wrote `support/archive/chat-112.md` (back-filled from chat-112 close deferral), wrote `support/archive/chat-113.md` (this), overwrote `support/next-chat-prompt.md` with chat-114 brief, overwrote `support/session.md` with chat-114 pointer, collapsed chat-112 IN PROGRESS + chat-113 IN PROGRESS sections in todo.md to closed stubs, added chat-114 IN PROGRESS placeholder. OPEN FOLLOW-UP carried forward: physics_bounce_friction catalog type_hint_coverage=none stale despite chat-107 lint pass annotating 4 blocks fully (catalog-flip-only-no-axis-refresh case); future cleanup chat should re-audit. See `support/archive/chat-113.md`.

---

## Chat 112 — ✅ closed at M2 via chat-114 M0 absorption (Phase-9 lint cohort 22; SOFT-DEFAULT graphics_shaders.html confirmed clean at M0)

`graphics_shaders.html` linted (Decision 3 NO-SKIP at 109 max-block well below 300-line monolithic threshold; monolithic-bucket SKIP streak PAUSED at 15 from chat-111; Decision 1 PASS canonical clean-no-mixed-finding TWENTY-FIRST consecutive with 2 classes ShaderRenderer+PostProcessing both retaining manual __init__ due to non-trivial side effects (init_render_setup + create_framebuffers respectively); Decision 2 SMALL sweep at 13 atomic edit pairs across Blocks 1-3 via single Filesystem:edit_file call dry-run-then-commit clean first-try; Block 1 ShaderRenderer 5 pairs (typing import + __init__ full-body-rewrite + 3 method returns); Block 2 PostProcessing 3 pairs (__init__ + create_framebuffer full-body-rewrites + create_bloom_shader return); Block 3 Practice Exercise example-solution 5 pairs (5 function signatures); 28 total annotations applied). NEW chat-112 PRECEDENT: pre-flight whitespace inspection on blank lines (8-space trailing-whitespace common in this codebase) via local bash repr() check on Claude's-copied file before staging multi-line anchors; avoids late-discovery anchor failures. NEW chat-112 PRECEDENT-EXTENSION: explicit Practice-Exercise example-solution Python blocks are LINTABLE under Decision 3 if under 300-line monolithic threshold; disambiguation from chat-58 do-not-modify-policy (which applies to EXERCISE PROSE not example-solution code per chat-86 Decision 3). Brief-vs-catalog axis discrepancy noted at M0: brief stated `type_hint_coverage=none`; actual catalog value was `partial`; disk confirmed catalog correct since Blocks 1+2 carried partial __init__ parameter annotations pre-edit; lint axis flip is `partial` → `full` (one-step bump either way; no functional impact). Catalog M1 flip landed via TWO-PHASE Filesystem:edit_file per chat-110+111 PRECEDENT: Phase 1 surgical (5 pairs ~1.5 KB for status/axes/lint_priority/stats); Phase 2 notes-embed single pair (7.8 KB). Both phases committed clean first-try. chat-104 axis-bump rule confirmed inverse case: Block 1 grew 101 → 102 lines from typing import addition BUT Block 1 is NOT the largest tracked block (Block 3 at 109 is) so NO axis bump applied (catalog `example_solution_size_lines` stays at 109, `all_blocks_largest_size` stays at 109). Graphics module 1/5 → **2/5 partial**. High-priority 18/24+ → 19/24+; cumulative 22 → 23 lessons linted. Stats final: by_priority high 4 medium 33 skip 27; by_lint_pass_status pending 37 + 23 linted_chat keys (added linted_chat_112: 1) + skipped 4; by_type_hint_coverage none 28 partial 22 full 14. Catalog file 282 KB → 290 KB (+8 KB from chat-112 notes embed). M0 disk-vs-catalog verification caught no drift: chat-111 flip confirmed landed on disk; broader sanity check on sprite_groups_layers chat-111 + sprite_animation chat-110 + polish_difficulty chat-96 all clean. M2 closeout for chat-112 hit tool-use cap before all housekeeping landed; archive deferred and back-filled at chat-114 M0 absorption; `support/chat-112-notes.txt` housekeeping artifact was claimed persisted but never landed on disk per chat-113 M0 finding (catalog-internal payload intact so low-priority artifact gap only). See `support/archive/chat-112.md`.

---

## Chat 111 — ✅ closed at M2 (Phase-9 lint cohort 21; SOFT-DEFAULT sprite_groups_layers.html confirmed clean at M0)

`sprite_groups_layers.html` linted (Decision 3 SKIP at 445-line monolithic Block 6 CompleteGroupDemo with 8 nested pygame.sprite.Sprite subclasses Player/Enemy/Bullet/Item/BackgroundElement/Explosion/HitEffect/CollectEffect plus 12 demo orchestration methods plus event loop, FIFTEENTH consecutive monolithic-bucket-SKIP matching chat-110 sprite_animation CompleteAnimationDemo shape but substantially larger at 445 vs 352 lines; Decision 1 PASS canonical clean-no-mixed-finding TWENTIETH consecutive matching chat-104/106/107/108/110 shape with 7 manual __init__ classes Enemy/LayeredSprite/GameLayeredGroup/SpatialHashGroup/CameraGroup/SpritePool/GroupManager all correctly retaining manual init (5 inherit pygame.sprite.Sprite/Group/LayeredUpdates requiring super().__init__() chain; SpritePool requires pre-population for-loop; GroupManager requires self-method-binding in collision_pairs list); Decision 2 LARGE sweep at 33 atomic edit pairs across Blocks 1-5 split into Group A 20 pairs (Blocks 1+2+3+4) + Group B 13 pairs (Block 5) per chat-105 LARGE-bucket precedent with NEW chat-111 PRECEDENT 20+13 split shape extending chat-110 17+16 demonstration, plus 3 from-scratch typing-import-line additions (Block 2 `from typing import Any, Optional`; Block 4 `from typing import Optional`; Block 5 `from typing import Any, Callable`); both groups dry-run-plus-commit clean first attempt = 4 Filesystem:edit_file invocations total). Split shape chose to isolate Block 5 GroupManager's 2 docstring-anchor disambiguation pairs (Block 6 has 8 update(self,dt) collisions + 1 check_collisions(self) collision; Block 7 has 2 update(self,dt) collisions) into Group B so any docstring-anchor issues would not block earlier-block landings. NEW chat-111 PRECEDENT: `pygame.sprite.AbstractGroup` Liskov-safe annotation for dict values mixing `pygame.sprite.Group` + `LayeredUpdates` + `GroupSingle` (all AbstractGroup subclasses); applied to GroupManager.groups annotation. NEW chat-111 PRECEDENT: `Callable[..., None]` for bound-method references in list-of-tuples value annotations (extends chat-110 `Callable[[], None]` event-callback pattern); applied to GroupManager.collision_pairs list. NEW chat-111 PRECEDENT: trim notes from initial draft to ~15-18 KB if pushing 25+ KB to stay within chat-110 19 KB precedent band (chat-111 initial draft 28 KB trimmed to final 15.3 KB by compressing duplicative Decision-1/Decision-3 audit sections + Group A/B manifests + carry-forward disciplines). chat-110 PRECEDENT pre-flight collision-check on Decision-3-SKIP block contents applied: scan of Block 6 + Block 7 identified 11 def update(self,dt) collisions + 1 def check_collisions(self) collision; both Block 5 GroupManager variants disambiguated via docstring-anchor strategy (3-line anchor including `"""Update all groups"""` plus `self.groups['all'].update(dt)` body line for update; 2-line anchor including `"""Check all collision pairs"""` for check_collisions). sprites module 1/2 partial → **2/2 complete = SIXTH complete Phase-9 module** joining architecture (chat-92) + polish (chat-100) + networking (chat-105) + game_mathematics (chat-106) + physics (chat-109 catalog-flip-only). High-priority 17/24+ → 18/24+; cumulative 21 → 22 lessons linted. Stats final: by_priority high 4 medium 34 skip 26; by_lint_pass_status pending 38 + 22 linted_chat keys (added linted_chat_111: 1) + skipped 4; by_type_hint_coverage none 28 partial 23 full 13. Catalog file 273 KB → 282 KB (+9 KB from chat-111 notes embed after trim). File grew 1451 → 1457 lines on disk. M0 disk-vs-catalog verification caught no drift: chat-110 sprite_animation flip confirmed landed cleanly (linted_chat_110: 1 + chat_110_lint_pass_notes 18564 chars + all stats matching); broader sanity check on sprite_animation chat-110 + polish_difficulty chat-96 + physics_gravity chat-109 all clean. OPEN FOLLOW-UP carried forward: physics_bounce_friction catalog type_hint_coverage=none stale despite chat-107 lint pass annotating 4 blocks fully (catalog-flip-only-no-axis-refresh case); future cleanup chat should re-audit. See `support/archive/chat-111.md`.

---

## Chat 110 — ✅ closed at M2 (Phase-9 lint cohort 20; SOFT-DEFAULT sprite_animation.html confirmed clean at M0)

`sprite_animation.html` linted (Decision 3 SKIP at 352-line monolithic Block 6 CompleteAnimationDemo + WalkingCharacter + FlyingCreature + PulsingUI + ExplosionEffect + ClickEffect + SimpleAnimation 8-class comprehensive pygame demo exercising all techniques from Blocks 1-5 in single runnable application, FOURTEENTH consecutive monolithic-bucket-SKIP matching chat-107 BounceAndFrictionDemo shape; Decision 1 PASS canonical clean-no-mixed-finding NINETEENTH consecutive matching chat-104 networking_sockets plus chat-106 game_mathematics_vectors plus chat-107 physics_bounce_friction plus chat-108 physics_collision_response shape; Decision 2 LARGE sweep at 33 atomic edit pairs across Blocks 1-5 split into Group A 17 pairs (Blocks 1+2+3) + Group B 16 pairs (Blocks 4+5) per chat-105 LARGE-bucket precedent, plus 3 from-scratch typing-import-line additions (Block 3 `from typing import Any, Optional`; Block 4 `from typing import Any, Callable, Optional`; Block 5 `from typing import Any, Optional`); both groups dry-run-plus-commit clean first attempt = 4 Filesystem:edit_file invocations total). NEW PRECEDENT: edit_file handles up to ~19 KB single-pair payload on current Filesystem MCP without Finding-19 timeout (contradicting older chat-99 ~10 KB threshold; likely older MCP infrastructure). NEW PRECEDENT: TWO-PHASE catalog flip via direct Filesystem:edit_file (alternative to chat-108 catalog_update.py script): Phase 1 small surgical edits (5 pairs ~1 KB for status/axes/lint_priority/stats) + Phase 2 large notes-embed single pair (~19 KB targeting unique _audit_extras tail); both clean first attempt. NEW PRECEDENT: Callable[[], None] for event callbacks (AnimationWithEvents events dict + add_event callback param). NEW PRECEDENT: pre-flight collision-check on Decision-3-SKIP block contents before staging Decision-2 annotation pairs to catch `def update(self):` / `def update(self, dt):` collisions; chat-110 caught Pair 19 AdvancedAnimation.update collision with Block 6 SimpleAnimation.update, resolved via 4-line extended anchor including `current_image, current_duration = self.frame_data[self.current_frame]`. NEW PRECEDENT: pygame.Surface / pygame.Rect annotations OK in blocks without explicit pygame import (forward-references not runtime-evaluated). sprites module 0/2 high-priority → 1/2 partial = FIRST sprites-module Phase-9 cohort. High-priority 16/24+ → 17/24+; cumulative 20 → 21 lessons linted. Stats final: by_priority high 5 medium 34 skip 25; by_lint_pass_status pending 39 + 21 linted_chat keys (added linted_chat_110: 1) + skipped 4; by_type_hint_coverage none 29 partial 22 full 13. Catalog file 254 KB → 273 KB (+19 KB from chat-110 notes embed). File grew 1313 → 1319 lines on disk. M0 disk-vs-catalog verification caught no drift: chat-109 flips (linted_chat_108 retroactive + linted_chat_109) both confirmed landed cleanly; broader sanity check on polish_difficulty chat-96 + networking_basics chat-103 + physics_bounce_friction chat-107 all clean. OPEN FOLLOW-UP carried forward: physics_bounce_friction catalog type_hint_coverage=none stale despite chat-107 lint pass annotating 4 blocks fully (catalog-flip-only-no-axis-refresh case); future cleanup chat should re-audit. See `support/archive/chat-110.md`.

---

## Chat 109 — ✅ closed at M2 (Phase-9 catalog-flip-only side-quest closing physics 3/3 = FIFTH complete Phase-9 module)

`physics_gravity.html` catalog-flip-only side-quest landed (re-audit refreshed example_solution_size_lines 429 → 108, all_blocks_largest_size 429 → 108, type_hint_coverage none → partial via pre-Phase-9 Block 4 5-typed-defs annotation pass at unknown chat-N, lint_findings emptied, lint_priority high → skip via override). PLUS chat-108 retroactive recovery: physics_collision_response chat-108 flip never landed on Ray WSL catalog pre-chat-109; chat-109 M0 caught the drift by surveying stats.by_lint_pass_status for missing-chat-108 key; chat-108-notes.txt preserved on disk at 17.5 KB used verbatim for retroactive flip. Scope B+ confirmed mid-M1 after M0 surfaced second drift case. NEW PRECEDENT: M0 disk-vs-catalog verification ESCALATED to also check that recent-chat flips actually landed on Ray WSL catalog (chat sequence by_lint_pass_status jumped 87/89/90/91/92/93/94/95/96/97/98/99/100/103/104/105/106/107 → 109 skipping 108 pre-chat-109). NEW PRECEDENT: push final catalog back to Ray WSL via Filesystem:edit_file batch (47-edit ~48 KB payload dry-run-plus-commit clean first attempt; 1 stats block replace + 2 full-entry replaces + 44 surgical insertions for schema-bump-gap pending entries + js_only skipped tags) rather than leaving canonical command for Ray. NEW PRECEDENT: broader sanity check at M0 sampling 2-3 linted entries' disk state. 44 schema-bump-gap pending entries gained explicit `lint_pass_status: pending` (entries audited at chat-86 baseline but never got chat-87-M0 schema-bump). 4 js_only entries gained explicit `lint_pass_status: skipped`. Physics module 2/3 partial → 3/3 complete = FIFTH complete Phase-9 module. Cumulative lessons linted: 18 → 20 (chat-108 retroactive + chat-109 catalog cleanup). Stats final: by_priority high 6 medium 34 skip 24; by_type_hint_coverage none 30 partial 21 full 13; monolithic_python_blocks_gt_300_lines 17. See `support/archive/chat-109.md`.

---

## Chat 108 — ✅ closed at M2 (Phase-9 lint cohort 19; SOFT-DEFAULT physics_collision_response.html confirmed clean at M0)

`physics_collision_response.html` linted (Decision 3 SKIP at 405-line monolithic Block 4 CollisionResponseDemo plus CircleObject plus PolygonObject comprehensive pygame demo bundling THREE collision-shape resolution variants circle-circle + circle-polygon + polygon-polygon plus interactive mouse-spawn plus keyboard handlers, THIRTEENTH consecutive monolithic-bucket-SKIP; Decision 1 PASS canonical clean-no-mixed-finding EIGHTEENTH consecutive matching chat-104 networking_sockets plus chat-106 game_mathematics_vectors plus chat-107 physics_bounce_friction shape; Decision 2 MEDIUM sweep at 20 atomic edit pairs across 4 blocks plus 3 from-scratch typing-import-line additions (Block 1 `from typing import Any`; Block 2 + Block 3 `from typing import Any, Optional` each) = approximately 53 total annotations applied (33 parameter + 4 instance-attribute + 16 return-type); 4 block groups × 2 dry-run-plus-commit = 8 Filesystem:edit_file invocations all clean first attempt). NEW PRECEDENT: catalog-flip-in-chat via local-script-execution (Claude container bash + Python + Filesystem MCP read access; script validates dry-run + commit byte-for-byte; canonical command left for Ray to run on user's WSL filesystem). NEW PRECEDENT: `Any` for duck-typed object parameters when no concrete class exists in scope (Protocol too heavy; string forward-ref technically incorrect); pygame.math.Vector2 for explicit-Vector2 params even when pygame import missing from block (pre-existing teaching-snippet convention; annotations not runtime-evaluated by default). oldText uniqueness verified 16 of 17 atomic pairs via simple single-line match; 1 pair (Block 5 Ball.update) required 3-line inline-comment-anchor (`# Four walls`) disambiguating from 3 same-signature def update methods in SKIP Block 4 (CollisionResponseDemo + CircleObject + PolygonObject). NO chat-106 split-strategy forward-reference applied (no central class library; each block standalone). NO Block-level Decision-2 locals-optional SKIP (each lint-pass block had at least one def or class). physics module 1/3 high-priority → 2/3 partial = SECOND physics-module Phase-9 cohort continuing chat-107-opened physics sequence. High-priority 15/24+ → 16/24+; cumulative 17 → 18 lessons. M0 disk-vs-catalog verification caught brief-drift: chat-106 + chat-107 pending catalog flips per chat-108 brief were ALREADY LANDED on disk at chat-108 M0 startup. Catalog update validated dry-run + commit in Claude container at chat-108 M1; Ray runs `python3 support/phase9_catalog_update.py physics_collision_response.html --chat 108 --notes-file support/chat-108-notes.txt` (17.5 KB notes) on user's WSL filesystem to land identical byte-for-byte result. See `support/archive/chat-108.md`.

---

## Chat 107 — ✅ closed at M2 (Phase-9 lint cohort 18; SOFT-DEFAULT physics_bounce_friction.html confirmed clean at M0)

`physics_bounce_friction.html` linted (Decision 3 SKIP at 373-line monolithic Block 4 BounceAndFrictionDemo plus PhysicsBall comprehensive pygame demo, TWELFTH consecutive monolithic-bucket-SKIP; Decision 1 PASS canonical clean-no-mixed-finding SEVENTEENTH consecutive matching chat-104 networking_sockets plus chat-106 game_mathematics_vectors shape; Decision 2 MEDIUM sweep at 21 atomic edit pairs across 4 blocks plus 1 from-scratch `from typing import Optional` import-line addition (Block 1 for handle_bounce Optional[float] return) = approximately 92 total annotations applied (44 parameter + 31 instance-attribute + 17 return-type); 4 block groups × 2 dry-run-plus-commit = 8 Filesystem:edit_file invocations all clean first attempt). NEW PRECEDENT: full-body-rewrite for __init__ methods provides natural class-specific disambiguation via instance-attr suffix (self.material vs self.on_surface vs self.angular_velocity vs self.bounces); docstring-anchor (Block 3 set_material with `"""Set material properties"""` + update with `"""Update physics"""` plus `# Apply forces` comment) plus inline-comment-anchor (Block 5 apply_force-update merged with `# Gravity each frame (chat-44 M2 pattern)` comment) handle method-signature collisions with same-signature methods in SKIP Block 4 (PhysicsBall.set_material + apply_force; BounceAndFrictionDemo.update). NO chat-106 split-strategy forward-reference applied (no central class library; each block standalone with only external pygame.math.Vector2 references). NO Block-level Decision-2 locals-optional SKIP (each lint-pass block had at least one def or class). physics module 0/3 high-priority → 1/3 partial = FIRST physics-module Phase-9 cohort = FIFTH Phase-9 cohort opening a new module joining chat-94 networking + chat-95 graphics + chat-103 networking-completion-pivot + chat-106 game_mathematics first-and-only. High-priority 14/24+ → 15/24+; cumulative 16 → 17 lessons. Catalog update bundled with M2 close report per chat-104 protocol; Ray runs `python3 support/phase9_catalog_update.py physics_bounce_friction.html --chat 107 --notes-file support/chat-107-notes.txt` (alongside pending chat-106 catalog flip). See `support/archive/chat-107.md`.

---

## Chat 106 — ✅ closed at M2 (Phase-9 lint cohort 17; SOFT-DEFAULT game_mathematics_vectors.html confirmed clean at M0)

`game_mathematics_vectors.html` linted (Decision 3 SKIP at 319-line monolithic Block 8 VectorGame pygame demo, ELEVENTH consecutive monolithic-bucket-SKIP; Decision 1 PASS canonical clean-no-mixed-finding SIXTEENTH consecutive matching chat-104 networking_sockets shape; Decision 2 MEDIUM-LARGE sweep at 36 atomic edit pairs across 8 blocks plus 1 from-scratch `from __future__ import annotations` import-line addition (Block 1) = approximately 85 total annotations applied (50 parameter + 35 return), falling just above the chat-105 30-pair LARGE threshold; 4 block groups × 2 dry-run-plus-commit = 8 Filesystem:edit_file invocations all clean first attempt). NEW PRECEDENT: split-strategy forward-references (central class library Block 1 received `from __future__ import annotations` for unquoted self-references; auxiliary Blocks 3-7, 9 used string forward-refs `"Vector2"`; Block 10 used PEP 585 lowercase `list[float]` for plain-list practice-exercise helpers). NEW PRECEDENT: Block-level SKIP under Decision-2 locals-optional clause for Block 2 (15-line module-level vector-arithmetic demo with zero def or class definitions). One diagnostic shift from brief premise: Block 1 turned out to be a 60-line Vector2 class library (not the monolithic) and Block 8 was the 319-line VectorGame pygame demo (the monolithic); SKIP rationale unchanged. game_mathematics module 0/1 → 1/1 = FOURTH complete Phase-9 module after architecture + polish + networking. High-priority 13/24+ → 14/24+; cumulative 15 → 16 lessons. Catalog update bundled with M2 close report per chat-104 protocol; Ray runs `python3 support/phase9_catalog_update.py game_mathematics_vectors.html --chat 106 --notes-file support/chat-106-notes.txt`. See `support/archive/chat-106.md`.

---

## Chat 105 — ✅ closed at M2 (Phase-9 lint cohort 16; SOFT-DEFAULT networking_lobby.html confirmed clean at M0)

`networking_lobby.html` linted (Decision 3 SKIP at 609-line NEW HIGH-WATER monolithic-bucket cohort, 145 lines above prior high from chat-103 networking_basics 464; TENTH consecutive monolithic-bucket-SKIP; Decision 1 PASS canonical mixed-finding-retained-under-override FIFTEENTH consecutive matching chat-94 partial-dataclass-plus-2-findings shape; Decision 2 = approximately 158 annotations across 84 atomic edit pairs spanning 5 block groups plus 1 from-scratch typing-import-line addition (Block 3) plus 1 in-place typing-import-line extension (Block 1) = NEW LARGEST Decision 2 sweep across Phase-9 history exceeding chat-104 record of 115 annotations / 34 pairs by 43 annotations and 50 pairs). NEW PRECEDENT: per-block-group dry-run-plus-commit pairs for LARGE sweeps (greater-than-30 atomic pairs); 4 block groups × 2 invocations = 8 Filesystem:edit_file calls all clean first attempt. Networking 4/5 partial (only medium-priority networking_client_server remains). High-priority 13/24+ to 14/24+; cumulative 15 to 16 lessons linted. Catalog update bundled with M2 close report per chat-104 protocol; Ray runs `python3 support/phase9_catalog_update.py networking_lobby.html --chat 105 --notes-file support/chat-105-notes.txt`. See `support/archive/chat-105.md`.

---

## Chat 104 — ✅ closed at M2 (Phase-9 lint cohort 15; SOFT-DEFAULT networking_sockets.html confirmed clean at M0)

`networking_sockets.html` linted (Decision 3 SKIP at 320-line SMALLEST monolithic-bucket cohort to date; NINTH consecutive monolithic-bucket-SKIP; Decision 1 PASS canonical clean-no-mixed-finding FOURTEENTH consecutive matching chat-99 shape; Decision 2 = 115 annotations across 34 atomic pairs plus 3 from-scratch typing-import-line additions = LARGEST Decision 2 sweep across Phase-9 history). NEW PRECEDENT: catalog `example_solution_size_lines` axis bumps under from-scratch typing-import-line additions (Block 1 320 to 321 on-disk; does NOT shift Decision 3 classification; catalog notes document this for future chat reference). PEP 585 lowercase `list`/`dict` pattern established for auxiliary-block annotations. Networking 3/5 partial. High-priority 12/24+ to 13/24+; cumulative 15 lessons. See `support/archive/chat-104.md`.

---

## Chat 103 — ✅ closed at M2 (Phase-9 lint cohort 14; pivoted from physics_gravity to networking_basics at M0 after catalog-stale finding)

`networking_basics.html` linted (Decision 3 SKIP at 464-line comprehensive client-server-API-surface bundle = LARGEST monolithic-bucket SKIP to date; Decision 1 PASS canonical mixed-finding-retained THIRTEENTH consecutive; Decision 2 = 42 annotations across 25 atomic pairs). FIRST production invocation of chat-102 `phase9_catalog_update.py` script (clean; stats refreshed first time since chat-86). Networking 2/5 partial. NEW finding: physics_gravity catalog-stale (on-disk Block 4 already distilled outside Phase-9; sampling confirmed isolated). High-priority 11/24+ → 12/24+; cumulative 14 lessons. See `support/archive/chat-103.md`.

---

## Chat 102 — ✅ closed at M2 (automation-infrastructure meta-chat; no lint cohort)

Wrote `support/phase9_catalog_update.py` replacing Finding-19 split-payload protocol for catalog M1 updates; extended `support/phase9_lint_audit.py` with `load_prior_chat_notes()` preserving ~96 KB of chat_NN_lint_pass_notes across audit re-runs; moved stale chat-64-startup one-shot `compact_todo.py` to `support/scripts-archive/`. See `support/archive/chat-102.md` for detail.

---

## Chat 101 — ✅ closed at M2 (cleanup chat; Finding-14 chat-100-M2 absorption + P0+P1 process trims)

No lint cohort work. Absorbed chat-100 M2 deferral; rewrote `todo.md` 430 KB → 16 KB via flat-index; created `support/closed-phases/` + `support/scripts-archive/` and relocated 14 closed-phase + one-shot artifacts; wrote `support/archive/chat-100.md` (6.8 KB) + `support/archive/chat-101.md` (7.8 KB). See `support/archive/chat-101.md` for detail.

---

## Chat 100 — ✅ closed at M2 via Finding-14 absorption at chat-101 M0

`polish_playtesting.html` linted (Decision 3 SKIP; Decision 1 PASS canonical mixed-finding-retained TWELFTH consecutive; Decision 2 = 27 annotations across 14 atomic pairs). **POLISH MODULE 5/5 COMPLETE** = SECOND complete Phase-9 module. High-priority 11/24+; cumulative 13 lessons. First application of chat-100 process cutover conventions. See `support/archive/chat-100.md`.

---

## Chats 94-99 — ✅ closed (Phase-9 lint-pass cohorts 7-12)

- **Chat 99:** polish_screenshake (334 lines; clean Decision-1-PASS without mixed-finding; 56-annotation/7-pair sweep; Finding-19 split-payload protocol DISCOVERED). See `support/archive/chat-99.md`.
- **Chat 98:** polish_sound (361 lines; THIRD consecutive monolithic-bucket-SKIP; dispersed-method-signature 14-pair sweep). See `support/archive/chat-98.md`.
- **Chat 97:** polish_tweening (342 lines; SECOND monolithic-bucket-SKIP; 12-pair Optional-wrap correction). See `support/archive/chat-97.md`.
- **Chat 96:** polish_difficulty (305 lines; FIRST monolithic-bucket-SKIP at the 5-line-over-threshold boundary case; 21-annotation/8-pair). See `support/archive/chat-96.md`.
- **Chat 95:** graphics_procedural (184 lines; opens graphics module 0/5 → 1/5; 25-annotation/7-pair). See `support/archive/chat-95.md`.
- **Chat 94:** networking_lag_compensation (202 lines; opens networking 0/5 → 1/5; 14-annotation/4-pair). See `support/archive/chat-94.md`.

---

## Chats 30-93 — flat-index

| Chat | Outcome | Archive |
|---|---|---|
| 30 | ✅ closed clean | _(git history)_ |
| 31 | ✅ closed clean | _(git history)_ |
| 32 | ✅ closed clean | _(git history)_ |
| 33 | ✅ closed clean | _(git history)_ |
| 34 | ✅ closed clean (across two attempts; second consecutive recovery exercise) | _(git history)_ |
| 35 | ✅ closed clean (across two attempts; third consecutive recovery exercise) | _(git history)_ |
| 36 | ✅ closed clean (FINAL Phase 7 chat — PHASE 7 CLOSED) | _(git history)_ |
| 37 | ✅ closed clean (Phase 8 kickoff: M2 + stage-38; pilot deferred to chat 38) | _(git history)_ |
| 38 | ✅ closed clean (Phase 8 pilot — pygame_basics_game_loop.html end-to-end) | _(git history)_ |
| 39 | ✅ closed clean (Phase 8 scaffold-script build — 63 lessons injected + Findings 7+8) | _(git history)_ |
| 40 | ✅ closed clean (Phase 8 question-content fill cohort 1 — pygame_basics module 5/5 done) | _(git history)_ |
| 41 | ✅ closed at M2 done (Phase 8 question-content fill cohort 2, partial — 2 of 4 sprite_* lessons… | _(git history)_ |
| 42 | ✅ closed clean (Phase 8 question-content fill cohort-2 finish — sprite_* module 4/4 done = 2nd complete Phase-8 module) | _(git history)_ |
| 43 | ✅ closed clean via interruption-recovery (Phase 8 question-content fill cohort-3 finish… | _(git history)_ |
| 44 | ✅ closed clean via interruption-recovery (Phase 8 question-content fill cohort 4 — physics_* module 5/5 done = 4th complete Phase-8 module) | _(git history)_ |
| 45 | ✅ closed clean (meta-chat: chat-44 M6 recovery + portable INTERRUPTION_RECOVERY.md + chat-46 stage; no cohort work) | _(git history)_ |
| 46 | ✅ closed clean — Phase 8 question-content fill (cohort-5 platformer foundational triplet: character_controller Finding-10 reconciliation… | _(git history)_ |
| 47 | ✅ closed clean — Phase 8 question-content fill (cohort-5 finish: parallax M1 standard authoring… | _(git history)_ |
| 48 | ✅ closed early at M1 via brief-documented drop-the-last-lesson safety-valve | _(git history)_ |
| 49 | ✅ closed clean — Phase 8 question-content fill (cohort-6-finish: polish_sound M1 + polish_tweening M2 standard authoring… | _(git history)_ |
| 50 | ✅ closed clean — Phase 8 question-content fill (cohort 7: polish_difficulty M1 + polish_playtesting M2 standard authoring… | _(git history)_ |
| 51 | ✅ closed early at M1 via brief-documented drop-the-last-lesson safety-valve | _(git history)_ |
| 52 | ✅ closed clean — Phase 8 question-content fill (cohort-8-finish: networking_sockets M1 + networking_client_server M2 standard authoring… | _(git history)_ |
| 53 | ✅ closed early at M1 via brief-documented drop-the-last-lesson safety-valve | _(git history)_ |
| 54 | ✅ closed early at M2 via brief-documented drop-the-last-lesson safety-valve | _(git history)_ |
| 55 | ✅ closed clean at M2 — Phase 8 question-content fill (cohort-10-finish-extended: architecture_scene_management M1 done… | _(git history)_ |
| 56 | ✅ closed clean at M2 (with mid-chat partial-disk-asymmetry at M1+staging-finish boundary absorbed via Finding-14… | _(git history)_ |
| 57 | ✅ closed clean at M2 (with mid-chat partial-todo.md-asymmetry at M1+staging-finish boundary absorbed via Finding-14… | _(git history)_ |
| 58 | ✅ closed clean at M2 (with mid-chat partial-bookkeeping-asymmetry at M1+staging-op-1-finish boundary absorbed via Finding-14… | _(git history)_ |
| 59 | ✅ closed clean at M2 (with mid-chat partial-bookkeeping-asymmetry at M1+catalog-flip-finish boundary absorbed via Finding-14… | _(git history)_ |
| 60 | ✅ closed at M2 (with mid-chat partial-bookkeeping-asymmetry at M1+staging-op-3-finish boundary absorbed via Finding-14… | _(git history)_ |
| 61 | ✅ closed clean at M2 — Phase 8 question-content fill (cohort-17: ai_behavior_trees… | _(git history)_ |
| 62 | ✅ closed at M2 (with mid-chat partial-bookkeeping-asymmetry at M1+catalog-flip-finish boundary absorbed via Finding-14… | _(git history)_ |
| 63 | ✅ closed at M2 (with mid-chat partial-bookkeeping-asymmetry at M1+catalog-flip-finish boundary absorbed via Finding-14… | _(git history)_ |
| 64 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-20: particle_effects… | [chat-64.md](support/archive/chat-64.md) |
| 65 | ✅ closed at M2 (with mid-chat partial-bookkeeping-asymmetry at staging-op-1-to-op-2 boundary absorbed via Finding-14 in-chat-re… | [chat-65.md](support/archive/chat-65.md) |
| 66 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-22: graphics_postprocessing… | [chat-66.md](support/archive/chat-66.md) |
| 67 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-23: graphics_ui_hud… | [chat-67.md](support/archive/chat-67.md) |
| 68 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-24: graphics_procedural… | [chat-68.md](support/archive/chat-68.md) |
| 69 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-25: graphics_shaders… | [chat-69.md](support/archive/chat-69.md) |
| 70 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-26: publishing_executables… | [chat-70.md](support/archive/chat-70.md) |
| 71 | ✅ closed at M2 (back-archived earlier; fat IN-PROGRESS section collapsed at chat-85 historical cleanup pass) | [chat-71.md](support/archive/chat-71.md) |
| 72 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-28: publishing_performance… | [chat-72.md](support/archive/chat-72.md) |
| 73 | ✅ closed at M2 end-to-end clean (collapse pass deferred to chat-75 startup as Op A under Finding-14… | [chat-73.md](support/archive/chat-73.md) |
| 74 | ✅ closed at M2 via Finding-14 cross-chat-startup absorption pattern | [chat-74.md](support/archive/chat-74.md) |
| 75 | ✅ closed at M2 end-to-end clean against the lesson target itself | [chat-75.md](support/archive/chat-75.md) |
| 76 | ✅ closed at M2 via Finding-14 cross-chat-startup absorption pattern | [chat-76.md](support/archive/chat-76.md) |
| 77 | ✅ closed at M2 end-to-end clean against the lesson target itself | [chat-77.md](support/archive/chat-77.md) |
| 78 | ✅ closed at M2 via Finding-14 cross-chat-startup absorption pattern | [chat-78.md](support/archive/chat-78.md) |
| 79 | ✅ closed at M2 via Finding-14 cross-chat-startup absorption pattern | [chat-79.md](support/archive/chat-79.md) |
| 80 | ✅ closed at M2 via chat-81 M2 TWO-CHAT batch (close-at-M0-only via Ray-directed chat-47 context-budget-flagging split at M0/M1… | [chat-80.md](support/archive/chat-80.md) |
| 81 | ✅ closed at M2 end-to-end clean via continuation chat after Ray-directed chat-47 context-budget-flagging split at NEW M1/staging… | [chat-81.md](support/archive/chat-81.md) |
| 82 | ✅ closed at M2 end-to-end clean via chat-82 continuation chat after Ray-directed chat-47 context-budget-flagging split at NEW post-staging-pre… | [chat-82.md](support/archive/chat-82.md) |
| 83 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-38: genres_tower_defense… | [chat-83.md](support/archive/chat-83.md) |
| 84 | ✅ closed at M2 end-to-end clean — Phase 8 question-content fill (cohort-39: genres_tower_defense_python… | [chat-84.md](support/archive/chat-84.md) |
| 85 | ✅ closed at M2 (twenty-second per-chat archive file = first cleanup-chat archive under chat-64-startup discipline) | [chat-85.md](support/archive/chat-85.md) |
| 86 | ✅ closed at M4 (twenty-third per-chat archive file = first Phase-9 archive under chat-64-startup discipline) | [chat-86.md](support/archive/chat-86.md) |
| 87 | ✅ closed at chat-88 M2 (twenty-fourth per-chat archive file = second Phase-9 archive joining `chat-86.md`) | [chat-87.md](support/archive/chat-87.md) |
| 89 | ✅ closed at M2 via Finding-14 cross-chat-startup recovery absorbed at chat-90 M0 startup | [chat-89.md](support/archive/chat-89.md) |
| 90 | ✅ closed at M2 across one mid-chat-resume slice point under Ray-directed "most efficient and less chat tokens" execut… | [chat-90.md](support/archive/chat-90.md) |
| 91 | ✅ closed at M2 end-to-end clean across compaction resume… | [chat-91.md](support/archive/chat-91.md) |
| 92 | ✅ closed at M2 across one compaction-recovery resume… | [chat-92.md](support/archive/chat-92.md) |
| 93 | ✅ closed at M2 via mid-chat Finding-14 absorption pattern… | [chat-93.md](support/archive/chat-93.md) |

> **Note on chat-30-63 sources.** Chats 30-63 predate the chat-64-startup archive-and-collapse discipline; no per-chat archive files exist for them. Full streaming-narrative content for those chats is recoverable from this file's git history (`git show HEAD~1:todo.md` immediately after this cleanup, or `git log -p -- todo.md` for the full window).

---

## Out-of-scope flags from chat 15 (decide separately if any are worth a focused chat)

1. **`details.toc-card` floating outline overlays canvas-fallback area on mobile.** Sticky `<details class="card toc-card auto-toc">`, z-index 100, ~229 px tall, default-open. Visually covers canvas-fallback figure on phones. Pre-existing (Phase 5 auto-TOC infra). Possible fixes: default-collapsed below 768 px, fully hidden below 768 px, or shrink the sticky height.
2. **Canvas demo JS animation loops keep running on mobile when canvas is hidden** — wasted CPU. Pre-existing across all 53 canvas demos. Easy per-lesson fix (`if (matchMedia('(max-width: 768px)').matches) return;` early-out at top of animation loop) but rolling out to 53 canvases is a script-pass.
3. **Lazy-load on canvas-fallback `<img>`** is correct behavior — noted only because it caused initial diagnostic confusion (`naturalWidth: 0` on first JS query, resolves on scroll). Implication for future render checks: query `naturalWidth` AFTER scrolling into viewport.

---

## Live open questions (full list incl. resolved in `status.md`)

1. `_python.html` companion files — standalone learning resources or appendix code dumps? (3 still missing both Summary + Exercise as of post-Phase-6 audit.)
2. `index.html` — join the design system (CSS vars, theme toggle) or stay bespoke?
